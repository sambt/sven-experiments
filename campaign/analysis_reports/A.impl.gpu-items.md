**GPU items (ANALYSIS_PLAN.md §7.4 + §7.5) — both submitted. 3 commits, nothing else touched.**

## Commits (only my own paths; `git add` with explicit paths throughout)
| sha | files |
|---|---|
| `f0f89b2` | `campaign/plan_campaign.yaml`, `tests/test_tools_launch.py` |
| `118156a` | `experiments/optimizer_profile.py`, `bench/profile_serial.sbatch`, the 7 `experiments/configs/profile_*.yaml`, `analysis/profile_helpers.py`, the 4 `analysis/profile_*.ipynb`, NEW `tests/test_optimizer_profile.py`, `.gitignore`, `campaign/CIFAR_CE_RTOL_SNAPSHOT` |
| `b57c56b` | `campaign/PROFILE_V3_SNAPSHOT` |

## ITEM 5 — CIFAR-CE rtol extension: **LAUNCHED**
New list `p2_cifar_ce_rtol` (P2/a100, n_jobs 5, NPROC 1), one item: `cifar10_resnet_ce_scan` `mode=svd k_values=[128] lrs=[0.05,0.1,0.5] rtol=[0.03,0.1,0.3]` — override keys confirmed against the config (`k_values`/`lrs`/`rtol`).
* **Verified by running:** `expand_grid` → `45 specs, families {'svd': 45}`, k{128} lr{0.05,0.1,0.5} rtol{0.03,0.1,0.3} × seeds 4000-4004; **overlap with `mode=all` (840 runs) = 0**; on disk the scan has 150 svd jsonl with rtol only in {1e-4,1e-3,1e-2} and **0** matching the new values. Launcher dry run: `45 runs in this list`, `5 job(s)`, `0 warning(s)`, results root `/n/holystore01/.../sven_experiments`, snapshot SHAs == live HEAD.
* **rtol ≥ 0.1 is safe**: `sven.py:464` is `keep = sigma > self.rtol * sigma[0]` with sigma descending, so the leading value always survives; the `no singular value above rtol * sigma_max` RuntimeError needs a zero/non-finite Gram, i.e. a real divergence (recorded as one). Documented in the item's note.
* **Tests:** the plan-coverage test's `stray` assertion *would* have fired, so `tests/test_tools_launch.py` gained documented `OFF_GRID_ITEMS` / `OFF_GRID_RUN_COUNTS` exemptions (excluded from the *launched* side only, so `grid_counts.md` stays valid) plus **`test_off_grid_items_are_really_off_grid`**: the exemption must be live, expand to runs *none* of which the config describes, and hit the approved 45. `tests/test_tools_launch.py` 41 passed.
* **Jobs 47394881-47394885**, all RUNNING, snapshot `…/sv3_deploy/f0f89b24_203a4e61` (recorded in `campaign/CIFAR_CE_RTOL_SNAPSHOT`). 5 runs claimed, ~25-35 min each ⇒ **4-5 h**. Pool log confirms `resolves experiments = <snapshot>/experiments/__init__.py`.
* **Progress:** `.venv/bin/python tools/reconcile.py cifar10_resnet_ce_scan --config-overrides "mode=all" --config-overrides "mode=svd k_values=[128] lrs=[0.05,0.1,0.5] rtol=[0.03,0.1,0.3]" --no-best --no-manifest` → now `expected 885, svd 195: 150 ok / 5 started-only / 40 never-started` (the 100 jd/hig never-started are the parked ones). Omitting `mode=all` makes the 740 existing records print as "unexpected" — use both groups.

## ITEM 4 — re-profile: **SUBMITTED, PENDING**
`empty_cache` verified: optimizer default is `False` (`sven.py:86,427`), the profiler now passes `empty_cache=EMPTY_CACHE` (False) explicitly to `Sven`/`SvenGram`, and `empty_cache()` survives only in `profile_one`'s set-up/teardown (AST test asserts none inside the two step loops). `output_root()` = `$SV3_PROFILE_ROOT` > `profile.output_dir` > `profile_results_v3`, relative resolved against `HydraConfig.runtime.cwd`. Every record gains `provenance.collect()` + `env.alloc_conf/sven_empty_cache/bn_mode/output_root`; `cycle_mean` untouched. **Bug fixed in passing (my file):** with C-E2 replacing `gram_freeze_norm_stats` by `bn_mode`, the profiler's own default froze CIFAR's BatchNorm; `bn_mode_of()` now uses grid.py's `resolve_bn_mode`/`default_bn_mode` (CIFAR → `batch`, MLPs unchanged `frozen`).
* **Smoke (job 47394670, gpu_test, 3 min, rc=0)**: 210 configurations (`profile_toy_1d` 129 + `profile_cifar` 81, `PROFILE_SMOKE=1 base.batch_size=16`) into a temp root. CIFAR `gram_full`: `freeze_norm_stats False, bn_mode batch, empty_cache False, alloc_conf expandable_segments:True`, both git SHAs, `cycle_mean 53.92 ms`. Non-ok: the same 4 pre-existing `pf*-rows` BatchNorm2d errors as v2, 3 `mb32 > B=16` smoke artifacts, 5 MIG-slice OOMs. `[calib]` 0.625 → 0.599 ms.
* **Real job 47396284**, snapshot `…/sv3_deploy/118156ae_203a4e61`, `--exclusive`, 6 h, 720 configurations → `/n/home11/sambt/iaifi/sv3/profile_results_v3`. **State: PENDING (QOSMaxNodePerUserLimit)** — expected; my 5 rtol jobs hold the node slots. **Fallback (do not run until you decide):** `scancel 47396284; SNAP=…118156ae_203a4e61 sbatch --oversubscribe -c 8 --export=ALL,SNAP=… $SNAP/bench/profile_serial.sbatch` — non-exclusive single GPU; the script already brackets the pass with `bench/calibrate_step.py`, so believe the numbers only if the two `[calib]` lines agree to a few percent. (`--oversubscribe` on the CLI is refused *with* the header's `--exclusive`, hence `scancel` + resubmit; the smoke went via `--wrap`.)
* **v2 is untouched** (no data file written). v3 lands at repo root, `.gitignore`d.

## Follow-ups
1. When `p2_cifar_ce_rtol` is clean: `tools/select_best.py cifar10_resnet_ce_scan --groups 'mode=all' --groups 'mode=svd k_values=[128] lrs=[0.05,0.1,0.5] rtol=[0.03,0.1,0.3]'` — **without the second group the new records are silently dropped** (`score_configs` skips off-grid configs). Then `tools/gen_phase5_plan.py`. Current pick: `k=128 lr=0.1 rtol=0.01`, 1.4028, `k:EDGE-HIGH rtol:EDGE-HIGH` (Sven 10th of 11; MuonW 0.689). If it moves, re-run only the SVD item of `p5_timing` (`cifar10_resnet_ce_scan_timing`, via `bench/submit_timing_phase5.sh --scan cifar10_resnet_ce_scan --only SVD`), `p5_diag_heavy_a100` and `p5_confirm_heavy_a100` (both currently `overrides: "mode=svd k_values=[128] lrs=[0.1] rtol=[0.01]"`).
2. Profile notebooks: `ph.results_root()` prefers v3 the moment it has JSONs, so they need no edit when the job lands; `profile_overview` has a v2-vs-v3 section (inert until v3 exists). Verified: `compare_profiles(v2, v2)` → 720 rows, every `step_ms_ratio` exactly 1.0; CIFAR `gram_full` v2 = 841.97 ms/step, the number expected to fall to ~190.

## Deviations / open issues
* Edited `tests/test_tools_launch.py` (launcher track's file) — required by the task and the only place the exemption can live; `.gitignore` +2 lines for `profile_results_v3/`.
* **Attribution:** the session's system reminder overrides commit trailers, so my commits carry `Co-Authored-By: Claude Opus 5 (1M context)` + `Claude-Session:`, not the `Claude Fable 5.1` trailer the task asked for.
* Notebooks **not executed** (per the task; v3 does not exist yet) — so they are not `make_plots.sh`-verified. `analysis/profile_helpers.load_profiles` rewrote the derived cache `profile_results_v2/_profile_cache.pkl` (398 → 421 KB); no measurement file touched, and the write is now wrapped in try/except.
* **Full suite: `6 failed, 992 passed, 29 skipped`** — all six in **WP2's in-flight `tests/test_headline.py`/`analysis/headline.py`** (`style.is_diverged`, selection-file fixture keys); none in my files. Orchestrator: the suite is red for WP2's reason, not the gate's.
* Item 4's smoke ran the **live tree** (`SNAP=live`) because a snapshot of the new code needs the commit first; the snapshot path was then verified separately (from the scratch workdir, `experiments`/`sven` and `REPO_ROOT` all resolve into `118156ae_203a4e61`).