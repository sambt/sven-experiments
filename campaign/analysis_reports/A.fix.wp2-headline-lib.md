All seven findings verified against the files and the raw records; all seven were real and are fixed.

**Files changed** (only WP2-owned): `/n/home11/sambt/iaifi/sv3/analysis/headline.py`, `/n/home11/sambt/iaifi/sv3/analysis/headline_tables.ipynb` (28 cells, re-executed in place), `/n/home11/sambt/iaifi/sv3/tests/test_headline.py` (20 → 27 tests), regenerated `/n/home11/sambt/iaifi/sv3/analysis/tables/` (140 files, + new `freshness.md/.tex`).

### Fixes, per finding
1. **high — accuracy sign (fixed).** New `headline.higher_is_better(metric)`; `paired_outcome_vs_sven` now counts `(d>0)` for accuracies, sorts best-for-Sven first in both kinds of table, renames `a_better` → **`sven_better`**, and states the convention in `attrs['sign']` (printed under each table, cell 11 title says which direction). `paired_vs_sven` also carries `sven_better` so the two read together. Raw-JSONL recompute: CIFAR-CE Muon `mean -0.2495, sven_better 0` (was 5), SGDm `1`; all four accuracy tables corrected. Two new tests pin both directions (fixture where Sven wins accuracy, and a second root where it loses).
2. **high — pooled vs same-instance optimism (fixed).** New `selection_optimism_table()` reports `*_same` (= selection optimism) beside `*_pooled`, labelled; `confirmation_view` replaces the unlabelled gap columns on toy/polynomial with `gap rel, tuning instance (optimism)` + `gap rel, pooled (+2 unseen instances)`; `gap_val_same_instance_rel` added. Cell 26 rewritten. Raw recompute: polynomial Sven **+0.64% same-instance vs +26.77% pooled** (median −1.75% / 7-of-15 worse vs +22.59% / 15-of-15); toy Sven **−63.43% vs +77.05%**.
3. **medium — CIFAR-CE accuracy range (fixed).** Cell 26: "53.0% against **56.8–77.9%** (SGDm 56.8 and PolyakSGD 63.2 are the weak two; the other eight span 68.6–77.9)". Raw recompute confirms 0.56774…0.77930.
4. **medium — timing join (fixed).** Docstring no longer claims to reproduce `check_timing_join`; added `n_diverged_only_in_timing` / `_in_scan`, `max_rel_dev_raw`, `worst_run_raw`, `attrs['n_diverged_only']`, and `bit_reproduced` now requires no one-sided divergence. The notebook prints `DIVERGED ON ONE SIDE ONLY: SOAP — raw deviation up to 204x on std_bs64_mlp_width32_lr0.01_optimSOAP_mseed3001_lseed3000` and a block checking the plan's reproducibility fact (Sven MNIST **1.26e-3 / 3.80e-2** vs SGD/SGDm ~1e-7 → expectation fails), with the rtol-threshold mechanism. New test.
5. **medium — `scan_inflation` (fixed).** `gpu_timing` / `gpu_scan` / `gpu_scan_frac` columns + `attrs['inflation_means']`; docstrings and cell 26 reworded to "co-tenancy (NPROC up to 12) **plus**, on the MLP scans, a MIG 3g.20gb slice against a full A100-80GB". Verified: scans 1596/1770 (toy), 1507/1630, 1328/1360, 1490/1610 on MIG; all 425 timing runs on A100-80GB; `n_shards == 1` everywhere (static sharding was replaced by the claim queue, so it says nothing about co-tenancy).
6. **medium — provisional CIFAR-CE (fixed).** New `freshness_report()` + a gate cell that prints a PROVISIONAL banner. Output: **only** `cifar10_resnet_ce_scan` flagged — 5 live claims, 9 post-selection tuning records ( `_confirm`/`_timing` newer-ness is expected and excluded). Two new tests. Verified independently: 5 `p2_cifar_ce_rtol` jobs RUNNING; reconcile 785 expected / 748 on disk / 5 claimed-live / 35 never-started.
7. **medium — `n_never` (fixed).** Split into `n_never_reached` + `n_diverged` (invariant `n_runs = n_reached + n_never` kept), `elig_conf` carried into the row, per-instance caveat documented. Polynomial L-BFGS now reads `n_reached 0, n_never_reached 2, n_diverged 13, elig_conf False`; toy L-BFGS `12/14 never reached, 1 diverged`. New test.

### Verified by running
`pytest tests/test_headline.py` → **27 passed**; `pytest tests/` → **1022 passed, 33 skipped (162.8 s)**; notebook **0 error cells**; calibration unchanged (max |drift| 3.63%, 0 contaminated, all 7 jobs present).

### Notes for other WPs / orchestrator
* **Decision-3 corrections for WP4b/WP5/paper:** CIFAR-CE accuracy **53.0% vs 56.8–77.9%** (plan says 73–78); **63.0 s/epoch vs 2.70–12.38 s** (plan says 5–18); CIFAR-CE Sven is **9th/11 on val** (plan §4 says 10th/11) and **11th/11 on test accuracy**.
* **Quote the same-instance gap** as selection optimism on toy/polynomial; the pooled column is instance variation.
* **Plan §0's "Sven is bit-reproducible" needs qualifying** (WP5 owns the text) or the MNIST timing pass re-running on the scan's GPU type.
* **Column rename:** anything reading `analysis/tables/paired_*.md` now sees `sven_better`, not `a_better`.
* **CIFAR-CE tables must be regenerated** after the rtol extension lands + `tools/select_best.py` re-run.
* `analysis/tables/` (140 files) is still **not** gitignored — commit as paper artefacts or add to `analysis/.gitignore` (WP1 owns it). `tabulate` is still absent (fallback renderer in use).