**FIXER, work package "gpu-items" — all 5 findings CONFIRMED and fixed; none rejected. 1 commit (`bfa7ca9`), 10 explicit paths, no job resubmitted.**

## Files changed
`campaign/plan_campaign.yaml` · `analysis/profile_helpers.py` · the four `analysis/profile_*.ipynb` (also **executed**) · `bench/profile_serial.sbatch` · `tests/test_optimizer_profile.py` · NEW `tests/test_profile_helpers.py` (14 tests) · `tests/test_analysis_offline.py` (one line — deviation below).

## Verified by running
**F1 (high) — destructive re-selection, real.** `select_best.py cifar10_resnet_ce_scan --out <side file> --groups 'mode=all' --groups 'mode=svd …'` → `[select] wrote …/ce_rtol.json`, payload `scans` = `['cifar10_resnet_ce_scan']` **alone**, vs the 7 scans in `bench/best_configs.json`. `--groups` is one tuple passed to every scan (`select_best.py:716`); `to_json` never merges (`:742-746`). `bench/best_configs.json` untouched (git-clean). The note now *requires* `--print` or `--out <side file>` + splicing the single key, says the file is git-tracked/recoverable, and records why re-running all seven is not the fix.
Bonus from that run: `745 record(s), 885 expected`; Sven still `k=128 lr=0.1 rtol=0.01`, 1.40281, 5/5 — but the edge flag is now **`k:EDGE-HIGH` only**: `rtol:EDGE-HIGH` is gone because 0.03/0.1/0.3 are in the expected grid. MuonW 0.6888.
**F2 (high) — false BN claim, real.** All 33 v2 CIFAR Gram records: `('cifar_resnet18','gram_full','False') 27`, `('…','gram_chunked','False') 6` — v2 already used batch statistics. Reworded in all four places (module + `compare_profiles` docstrings, `profile_overview` cell 14, `test_optimizer_profile` docstring): v2 got **two** settings wrong; `bn_mode_of` prevents a *future* post-C-E2 regression. Commit `118156a`'s message cannot be amended — `bfa7ca9` is the correction of record.
**F3 (medium) — cache in a read-only root, real.** `_cache_path()` → `$SV3_PROFILE_CACHE_DIR` else `experiment_results/_cache/profile_<root>.pkl`; root path added to the signature; `except Exception`; `compare_profiles` resolves ROOT_V2/V3 at call time (as a default arg `old` ignored any redirect — found by a test). After executing all four notebooks: `profile_results_v2/_profile_cache.pkl` mtime still **18:39:06** (untouched), new cache at `experiment_results/_cache/profile_profile_results_v2.pkl` 19:12.
**F4 (medium) — partial-v3 gate, real.** `profile_status()` → `(complete, n, 720)` from per-config `N_EXPECTED` **or** the new `_profile_complete` sentinel the sbatch writes after a clean full pass. Real v2 → `(True, 720, 720)`; 719/720 → `(False, 719, 720)`; partial v3 prints `is PARTIAL (4/720 configurations) -- staying on profile_results_v2`; `load_profiles` prints `PARTIAL, n/720`.
**F5 (medium) — unexecuted notebooks, real.** All four executed in place: `0 error cells, exec_count None []`. Cell 0: `profile results: …/profile_results_v2  (v2)` / `720 configurations ({'ok': 709, 'error': 8, 'infeasible': 2, 'oom': 1})`; last cell: `nothing to compare yet (old=ok profile_results_v2, new=missing profile_results_v3)`.
**Tests:** `tests/test_profile_helpers.py` 14 passed; full suite via `campaign/run_cpu_tests.sh` → **1015 passed, 33 skipped, exit=0** (WP2's 6 failures are gone).

## Jobs (checked `squeue` first; nothing submitted)
`47394881-85` all **RUNNING** ~40 min, snapshot `…/sv3_deploy/f0f89b24_203a4e61`. reconcile (`--config-overrides` both groups): svd `expected 195, ok 155, claimed-live 5, never-started 35` (+100 parked jd/hig) → 5 of 45 done, on track for ~4.5 h. `47396284` still **PENDING (QOSMaxNodePerUserLimit)**, snapshot `118156ae_203a4e61`, → `profile_results_v3/`. **I did not resubmit it:** its snapshot measures exactly what the plan asked, and my sbatch change is comments + the sentinel, so the count rule (tested) is what certifies this pass. Fallback unchanged and still not mine to run.

## Deviations / open issues
* `tests/test_analysis_offline.py` +4 lines (`$SV3_PROFILE_CACHE_DIR` redirect): without it that test's tmp roots now drop pickles into the shared `experiment_results/_cache/` — I created and deleted one proving it. Minimal shared-helper fix.
* Attribution: session reminder trailers (`Claude Opus 5 (1M context)`), not `Claude Fable 5.1`.
* **Left in place deliberately:** the orphaned `profile_results_v2/_profile_cache.pkl` (420 KB, gitignored, no longer read). Deleting it would be another write to a read-only root — orchestrator's call.

## For the other packages
* **WP1/WP5:** no pinning needed while `47396284` runs — `./make_plots.sh` stays on v2 and says so. Re-run the four PROFILES notebooks once v3 reads `720/720`. `plots_v2/profile/*.pdf` regenerated from v2.
* **WP2 / whoever re-selects:** read the plan note before touching `bench/best_configs.json`; if CIFAR-CE Sven moves, re-run only `bench/submit_timing_phase5.sh --scan cifar10_resnet_ce_scan --only SVD`, `p5_diag_heavy_a100`, `p5_confirm_heavy_a100`.