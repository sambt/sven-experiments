All seven findings verified against the raw records and fixed.

**Files changed** (WP5-owned only; nothing committed, no result directory touched)
- `/n/home11/sambt/iaifi/sv3/EXPERIMENTS.md` (+849 / −196)
- `/n/home11/sambt/iaifi/sv3/README.md`
- `/n/home11/sambt/iaifi/sv3/analysis/RERUNS_NEEDED.md`

**Findings: all 7 accepted, none rejected.**

1. **HIGH, §7 item 2 (Sven robustness).** Confirmed. Recomputed with `style.is_diverged`: Sven **688 of 7,825** on-grid runs in **16 of 21** scans (39 recorded). Rewrote item 2 with a per-scan Sven table and the mechanism: blow-ups concentrate at the **bottom** of each `rtol` grid (`toy_1d_scan` 194/900 — rtol 1e-6: 94, 1e-5: 70, 1e-4: 30, **none** at 1e-3/1e-2; `rebuttal_overparam_polynomial_scan` 252/1800; `rebuttal_overparam_toy_1d_scan` 111/720, all rtol 1e-4). Replaced the §7 table with a **both-counts** table (recorded · wide · per-method), added the missing `toy_1d_scan` 289 vs 82 row, and qualified §3.3 (Fig-5 2/15), §3.4 (κ 10/210), §3.5, §3.6 and §2's `ok/attempted` column. Tool cross-check: `select_best.py` prints `289 diverged … {'SVD': 194, …}` for toy and `33 … {'SGDm': 15, 'SGD': 13, 'SVD': 5}` for CIFAR-LR. **Table columns now sum exactly to 1,402 / 2,553 / 23,215.**
2. **§6 storage.** Confirmed; replaced the estimate with `stat`/`du`: toy log ladder 34 states / **199,445 B** (poly 208,037), 200-epoch overparam 201 states / **1.19–1.26 MB**, MNIST svd **3,979,860 B** mean and `mnist_scan_labelRegression/ckpt` = **2.63 GB** (not 9.2 GB — that is the campaign-wide ~2,490-run total), nanoGPT **183,277,320 B**, ResNet `final` 44.82 MB, GPT-2 702,845,528 B; campaign-wide **243 GB ckpt + 4.6 GB diag**. Root cause named (per-state `torch.save` overhead, ~2.1×) and added as new §9 item 7.
3. **§4 vs §6 CIFAR ladder.** Confirmed: the diag ladder that ran is **21 states / 940,996,941 B = 0.94 GB** (`epochs`), so 0.95 GB was the chosen option. §4 now reads "21 states, 0.94 GB … `log` would be 34 states / ~1.5 GB" (34 verified from a toy `log` file).
4. **Phase windows.** Confirmed. Row 1 end → **09-20 02:10:43Z** (job 47080834, cifar-CE); row 2 start → **09-20 03:34:11Z**. Added a stated convention (records' own times, UTC = EDT+4) and a note that "05:36" appears in no record. `RERUNS_NEEDED` row 4 → **04:00 → 10:39 EDT**; rows 1–3 also snapped to record spans in EDT.
5. **§9.3 CIFAR rank.** Confirmed. Now gives both: label-reg **9th/9th** (0.480345 / 0.483824), CE **10th tuning / 9th confirmation** (1.402812 → 1.423851 vs SGDm 1.431115, 0.5%), with §5's rule making 9th the publishable CE number.
6. **§7 mechanism.** Confirmed exactly: **1,147** `DivergedError` (with step) vs **275** without — **243 K-FAC `_LinAlgError`**, **32 Sven `RuntimeError`** ("SvenGram: no singular value above rtol…"). Added, plus the warning that a `diverged_at_step` histogram drops 19%.
7. **§4 pass divergences.** Confirmed 20 recorded / 21 wide. Added a table; `polynomial_scan_confirm` L-BFGS `lr 1.0, mi 1, hs 2` is **13/15** (4, 4, 5 per data seed) and was already `n_ok: 3, n_diverged: 2` on tuning seeds.

**Verified by running:** `pytest tests/test_configs.py` → **364 passed, 10 skipped** (twice). `select_best.py --compare-analysis --print` → `85 selection(s), 0 unverified, 0 error(s)`, `no disagreements`, 6 vs reconcile (no write: `generated_at` unchanged, clean vs HEAD). `reconcile.py` — campaign: `35 run(s) to do; incomplete: cifar10_resnet_ce_scan` (785 expected, 745 on disk, 5 claimed-live, 35 never-started); phase5: `21 scan(s), 0 run(s) to do`; **gpt2: now `0 run(s) to do`, 29/29 `ok`** (was 28+1). Machine re-check of all 21 §7 rows against raw jsonl → **0 mismatches**; markdown table column counts → clean in all three files.

**Two live-state changes since the implementer's report:** GPT-2's last SOAP run finished (**29/29**, window now → 09-20 23:13:06Z) and the CIFAR-CE extension has landed **7 of 45** (sha `f0f89b24`, jobs 47394881–85). The §7 CIFAR-CE row is pinned to its **740 on-grid** runs so it cannot go stale; the in-flight figures are explicitly timestamped (19:21 EDT) and marked as the one scan whose counts still move.

**Deviations / open issues.** No `CLAUDE.md` exists (not created). **No test added:** WP5-docs owns no test file, and a docs-consistency test would have to read the real results, which `ANALYSIS_CONTRACTS` forbids for unit tests — I ran the equivalent checks in-session instead (column sums, per-row re-derivation, table integrity). `tests/test_configs.py:723`'s comment still cites the stale `EXPERIMENTS.md:108-109`; that file is not mine (WP1 owns the test tree) — content now lives in §3.3 and §9.1.

**Notes for other WPs.** **WP2/WP4b:** publish CIFAR-CE as **9th of 11** (confirmation) and say so; polynomial L-BFGS confirmation is **2/15** — print the fraction or call it a failure. **WP1/WP2/WP4a:** every `finished/attempted` must use `is_diverged`, not `status` — toy-1D is 1,481 usable, not 1,688. **WP4a:** the κ study loses one *matched* cell (κ=2, lr 1.0, k 64 → 4/5); Fig-5 loses one seed each at pf 0.05 and 0.1. **Orchestrator:** paper text still uncorrected at `iclr2026_conference.tex` 90/290/387/846/692 and `WorkingNotes/main.tex:253`; `bench/best_configs.json` must be regenerated once the CIFAR-CE extension completes.