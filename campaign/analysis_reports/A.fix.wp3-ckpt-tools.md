**WP3 tool half — fixer report**

## Files (all WP3-owned, still untracked; nothing else edited, results untouched)
`/n/home/anon/sven-experiments/analysis/ckpt_tools.py` (960→1068 l), `/n/home/anon/sven-experiments/tools/compute_ckpt_spectra.py` (260→309 l), `/n/home/anon/sven-experiments/tests/test_ckpt_tools.py` (28→36 tests). Cache `analysis/ckpt_spectra/` unchanged (64 npz, 54 MB — verified byte-age and value-identical, see below).

## All 7 findings reproduced; all fixed
1. **Dataset cache (high) — real.** Reproduced: `toy_1d_scan_confirm` data_seed 1000/1001/1002 all returned `id=22376819597936`, `split_seed=1000`, `train[0]=(-0.584906,-0.207050)`. Fix: cache key + `resolved_config`'s guard now use `_resolved_subtree()` = `OmegaConf.to_container(resolve=True)`; `Run.dataset` also checks `split_seed` against the record. After: `(-0.584906,-0.207050)/(0.543525,0.329180)/(0.362286,1.345471)`, distinct objects, `verify_checkpoint` rel 6.7e-06/8.9e-06/3.6e-06 (abs ≤ 6.1e-11). **Sweep over all 43 scan dirs × 12 sampled runs: 0 failures** (was 8/12 in two confirm scans, hard error in the 3 overparam scans); all four `n_data` arms of both overparam scans rebuild with the right `n_train`.
2. **Report numbers (high) — real.** Recomputed from the delivered npz (seed means, final ckpt, `Σ_{i<k}utr²/Σutr²`, k from the record): polynomial Sven **k=16: 55.11 %→58.82 %**, k=32 73.71 %, k=105 89.59 %; MNIST probe512 **99.946 %→99.868 %**, probe2000 99.959 %→86.35 %; toy k=32 100.0000 %; rtol=0.03 keeps 12.4→104.6 of 673. So the old "74 % / 89 % / 92.9 %" were the k=32, top-105 and no value at all. **Corrected mechanism line: k=16 truncation is where polynomial loses signal (41 % of the residual energy sits outside it), not k=32.** The table in the old report is otherwise right — I re-derived σ_max 219.5→1.826/3.60/15.8/0.159, σ_B/σ_1 8.594e-17→1.300e-08 (Sven)/1.332e-07 (Adam), ‖Δθ‖ 7.82/7.02/6.26/8.52, toy SVs above the 1e-7 floor 8.2→27.4 (Sven)/30.6 (Adam).
3. **`verify_checkpoint(step=)` (medium) — real** (measured rel 7.35e+06 at step 64). Now maps an epoch-end step to its epoch, accepts step 0 as the untrained point (`epoch=-1`), and rejects a mid-epoch step with the list of verifiable steps. After: step 0 rel 2.4e-10, step 312 (epoch 0) 9.5e-08, step 6240 (epoch 19) 3.9e-05.
4. **CLI died on diverged runs (medium) — real.** Now try/except → `verify_rel_error=nan` + a `verify_error` string in the npz; re-ran `--methods LBFGS --seeds 2000,2004`: mseed2000 warns and writes 9 ckpts, **mseed2004 still runs** (verify 8.8e-09), exit 0.
5. **Unresolved config comparison (medium) — real** (two configs differing only in `${data_seed}` compared equal). Fixed with the same helper.
6. **Per-run RowSpec (medium) — real.** `checkpoint_spectra` now takes `spec=`, defaulting to `row_spec_for_scan`, which reads *every* `svd_` record and **raises** on disagreement; CLI prints the row definition per scan and gains `--row-spec-run`. `mnist_kappaScan_labelRegression` now refuses (3 definitions) instead of silently picking κ=2. The three diag scans agree, so **the delivered cache is still exact**: recomputed 3 steps × 2 methods × 3 scans, max rel diff 1.1e-09, probe_loss identical.
7. **Overstated verification claims (medium) — real.** Recomputed: toy Sven online-vs-offline Gram at the 1e-6·σ_max threshold is **2.4e-04 at step 0** (4.1e-04 step 1, 1.2e-03 step 64); **above 1e-2·σ_max it is ≤5e-07 on all three scans** (poly/MNIST ≤2.3e-06 even at 1e-6). Correct statement: the leading SVs agree to ~5e-07; below ~1e-2·σ_max the float32 Gram, not the tool, sets the floor. Abs errors measured over 7 diag passes: **MLP/LM max rel 7.4e-03 (HIG, abs 4.4e-11) / max abs 1.9e-06 (mnist-CE SOAP, loss 2.48); CIFAR max rel 4.1e-05 / max abs 1.8e-05** — the old "abs ≤ 2.2e-8 everywhere" was wrong.

**Extra fix:** the npz path is (method, seed, probe), so a non-diag scan's grid would overwrite one file — now detected and skipped with a message (exit 1). Test added.

## State
`tests/` green on a CPU job: **1033 passed, 33 skipped** (119 s); `tests/test_ckpt_tools.py` 36 passed.

**Other WPs / orchestrator:** cache API unchanged (`load_spectra`), no regeneration needed; quote the corrected §2/§7 numbers above; `analysis/sv_diagnostics.py` and `comparisons.ipynb` (phase B) are untouched. Heads-up: the shared scratchpad `/tmp/.../scratchpad/` contains another agent's `sven.py`, which shadows the real package for any script run from there — my scripts live in `/n/home/anon/sv3_wp3_scratch/fix/`.