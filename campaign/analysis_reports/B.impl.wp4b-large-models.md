All three notebooks execute clean and every headline number reproduces from the raw JSONL. Final report:

**Files created:** `/n/home11/sambt/iaifi/sv3/analysis/large_figs.py` (~760 l), `/n/home11/sambt/iaifi/sv3/tests/test_large_figs.py` (30 tests), `/n/home11/sambt/iaifi/sv3/analysis/gpt2_analysis.ipynb`. **Rewritten:** `/n/home11/sambt/iaifi/sv3/analysis/cifar_analysis.ipynb`, `/n/home11/sambt/iaifi/sv3/analysis/nanogpt_analysis.ipynb`. Builders (gitignored scratch): `agent_lab/wp4b/build_{cifar,nanogpt,gpt2}.py`, `nbreport.py`. **No shared helper and no `make_plots.sh` touched** (`git diff HEAD` on style/analysis_helpers/scan_analysis/headline/paired/budget/ckpt_tools/make_plots.sh is empty); nothing written into `analysis/tables/`.

| notebook | cells (code) | errors | runtime | figures (PDF+PNG) |
|---|---|---|---|---|
| `cifar_analysis` | 28 (16) | **0** | ~45–75 s | 15 → `plots_v2/cifar/` |
| `nanogpt_analysis` | 18 (10) | **0** | ~30 s | 4 → `plots_v2/nanogpt/` |
| `gpt2_analysis` | 13 (7) | **0** | ~30 s | 4 → `plots_v2/gpt2/` |

`pytest tests/test_large_figs.py` → 30 passed; full `tests/` → **1106 passed, 33 skipped** (126 s). 0 `[style]`/AVERAGED-OVER lines in any output.

**CIFAR (both scans, 11 methods, confirm/timing/diag).** Gate cell asserts `bn_mode == batch` on all 600 + 765 records and prints the PROVISIONAL banner (CIFAR-CE: 5 live claims, 22 post-selection records). Confirmation: label-reg Sven **9/11 val, 9/11 test acc**, 0.4838±0.0105 val, test acc **69.4%** vs 45.6–77.9 (MuonW 76.3, SOAP 77.9); CE Sven **9/11 val, 11/11 test acc**, 1.424±0.0126, acc **53.0%** vs **56.8–77.9** (SGDm 56.8 / PolyakSGD 63.2 weak, other eight 68.6–77.9) — phase-A corrections reproduced exactly. Optimisation-vs-generalisation (`train_eval` beside val): label-reg Sven **0.0674** (better than the method median 0.119) with val 0.484 → generalisation failure; CE Sven **0.819** vs median 0.096 → under-optimisation. Cost (standalone): Sven **62.9 / 63.0 s per epoch = 22.9x / 23.3x SGD**, 177 ms/step, **22,965 MB = 57x** the leanest (403 MB); baselines 2.70–12.38 s. Diag: mean rank inverted **112.5/128 (87.9%)** label-reg, **123.3/128 (96.3%)** CE — `k`, not `rtol`, binds. Robustness: Sven **5/90** diverged label-reg (all lr 1.0, rtol 1e-4), **0/195** CE. Landscape panels share one colour scale and one (lr, rtol) frame; the selected cell is ringed, not-eligible cells dashed + `*`.

**Fig 5** (`rebuttal_fig5_cifar_paramfrac_scan`, 3 seeds, actual fraction on x): f=1 val 0.668, test acc **74.1%**; f=0.5 0.767 / **72.9%**; f≤0.25 collapses to **10.0–17.8%** against 10% chance, with **2/15 diverged** (wide rule; `status: ok` — the table shows val[0], val[-1], ratio). Cost: **178.3 ms/step unmasked vs 342–389 ms masked (1.9–2.2x SLOWER)** while peak memory only falls to **0.51x** — the honest note, stated with the mask-resampled-every-step statement. Profile overlay reads `profile_helpers.results_root()`, so it picks v3 up automatically (v3 is still empty; job 47396284 PENDING); the cell prints that v2's 845.7 ms/step at f=1 is `empty_cache`-inflated and not comparable.

**nanoGPT.** Sven **2/5 val, 3/5 test**; val 1.715±0.008 (ppl 5.56) vs AdamW 1.713 — paired diff **+0.0022, CI [−0.023, +0.027], 3/5 seeds to Sven**, i.e. a tie; significant wins over Muon/MuonW/SOAP (−0.095…−0.102). Cost 2.40 s/epoch (2.6x AdamW), 615 MB, 20.9 ms/step. Time-to-target: median target reached in 21.6 epochs / 51.9 standalone s (AdamW 18.4 / 16.8); hard target unreached by all 5 methods. Timing pass reproduced the scan **bit-exactly (max_rel_dev 0)** for every method. Sven 0/20 diverged; k=B=64 flagged as a natural maximum.

**GPT-2** (29 runs, 1 seed, 1 epoch = 13,125 steps, B=k=16, step curves via `eval_step_idx`). Sven val **5.198 (ppl 181) / test 5.107** vs Muon 3.741, MuonW 3.846, AdamW 3.925, SOAP 3.935 — **+39%, last of 5**. Cost **9.25 h/run (3.20x fastest), 2517 ms/step, 36,050 MB (1.46x)**. Sven's lr optimum is **interior** (0.1 of 0.02–1.0) → the untuned knob is k=B=16, not lr. Asserts the results root is not legacy (the 4 contaminated records are never read).

**Deviations.** (1) `savefig` (PDF+PNG) lives in `large_figs`; `Scan.save` writes one format and is frozen. (2) Curves come from `headline`-selected runs, not `scan_analysis.plot_best_curves`, so they cannot drift from the tables. (3) Time axes use the timing pass, not the sharded confirm runs. (4) Deleted 4 stale gitignored PDFs (`plots_v2/cifar/paramfrac.pdf`, `plots_v2/nanogpt/standalone_*.pdf`) that no cell regenerates. No shared-helper bug found, so none fixed.

**Open issues.** CIFAR-CE is still landing (765 records; k=128 lr=0.1 rtol=0.1 → **1.358** and lr=0.5 rtol=0.3 → **1.349** already beat the pick 1.4028, and their accuracies are ~0.53→0.59): after `tools/select_best.py` (both groups) re-run `cifar_analysis` only — no prose or figure names a configuration. `scan_analysis.load_scan` prints "765 of 635 intended runs" on that scan (off-manifest extension); the cell explains it.

**make_plots.sh:** uncomment `gpt2_analysis` in `NEW=()`; `cifar_analysis` and `nanogpt_analysis` are already in `STUDIES`.