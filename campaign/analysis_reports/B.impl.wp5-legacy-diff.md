**Files created** (all WP5-owned; nothing committed, results roots untouched):
`/n/home11/sambt/iaifi/sv3/analysis/legacy_diff.py` (951 l) · `/n/home11/sambt/iaifi/sv3/analysis/legacy_vs_fresh.ipynb` · `/n/home11/sambt/iaifi/sv3/analysis/WHAT_CHANGED.md` · `/n/home11/sambt/iaifi/sv3/tests/test_legacy_diff.py` (20 tests) · `analysis/tables/legacy/` (13 md + 13 tex) · `analysis/plots_v2/legacy_vs_fresh/*.{pdf,png}` (2 figures).
**Changed:** `analysis/legacy_repair/corrections.csv` (8 → 21 scans) + 13 new repair parquets — the task's instruction to regenerate missing example-weighted repairs (`repair_legacy.py` via `run_cpu_tests.sh`; nanoGPT needed no `--n-val`, n_val = 871 from the records). No shared helper touched, **no bug fixes needed in frozen modules**.

**Notebook `legacy_vs_fresh`: 34 cells, 18 code, 0 error cells, 0 unexecuted, 42 s** (scans slim-cached; legacy cache goes to `analysis/legacy_repair/_cache`, never the read-only root). `pytest tests/` → **1155 passed, 33 skipped**.

**Method.** Legacy metric = example-weighted repair joined on `run_id` (exact: `eq_check_max` < 2e-5 on all 21 scans; coverage 100 % of usable legacy runs); legacy selection = the binding rule applied to legacy data; fresh = `bench/best_configs.json` via `headline.py`, never re-derived. Gate: my rule reproduces **84/85** selections; the one miss (CIFAR-CE Sven, 1.356 vs 1.403) is asserted to be confined to the scans `freshness_report()` flags — CIFAR-CE only, which prints a PROVISIONAL banner and whose every cell recomputes. 27 scans in both roots → 7 headline, 4 swept, 10 Sven-only, 6 excluded (GPT-2 quarantined: legacy val is a prefix of train).

**Key numbers.**
*Sven rank (methods in both roots), legacy → fresh:* toy 2/14→2/14; **polynomial 8/14→2/14 (−6**, ranks only: additive cubic); MNIST-LR 4/13→3/13; MNIST-CE 5/13→4/13; **CIFAR-LR 2/6→5/6 (+3**, 0.4107→0.4838, **+17.8 %**); CIFAR-CE 5/6→5/6 (1.415→1.424); nanoGPT 2/5→2/5. Full field: 2nd/15, 2nd/15, 3rd/14, 4th/14, 9th/11, 9th/11 val + 11th/11 acc (provisional), 2nd/5. Over 26 comparable points (7 + 19 axis): **12 better, 8 same, 6 worse, mean −0.69**. Swept: MNIST P/N 2,1,3,4,3 → 1,1,1,2,2; poly P/N 2,2,2,2 → 1,2,2,4; toy P/N 2,2,1,4 → 2,4,3,3; poly batch 9,6,7,1,2,1 → 3,1,1,1,5,4.
*Tested causes:* **BatchNorm** — CIFAR-LR Sven +17.8 % on an **unchanged grid (18→18)** while Adam +1.0 %, SGD +1.7 % at the same selected lr; CE Sven only +0.6 % (already 5th/6): the "Sven ≈ Adam on CIFAR" claim was a label-regression BN artefact. **Split** — MNIST Adam/SGD/RMSprop/Shampoo keep the **same lr in 8/8** pairs though grids doubled; levels shift 0.8–18.5 % (median 7.3 %), ranks ≤3. **Invisible failures** — legacy lost 16 seed-runs of observed configs (HIG 6 with 4 recorded divergences, KFAC 10 with 12; legacy toy HIG 35 runs/8 configs, **0** failures recorded vs fresh 210/210, 42 configs, 11 diverged). **AdamW=Adam** — **394/394** paired `wd=0` runs bit-identical in the legacy rebuttal scans; 0/394 at wd 0.01. *Repair sizes:* 0.06–0.42 % median headline, **8.6 % median / 52.7 % max on the masked CIFAR Fig-5 runs**; Fig-5 Sven at pf=1 0.418 → 0.668 (+59.8 %).
*Figures:* `sven_rank_legacy_vs_fresh` (slope chart, blue=better/red=worse, ranks fanned per group) and `method_levels_legacy_vs_fresh` (legacy grey vs fresh per method on both CIFAR + both MNIST scans).

**Claims** (WHAT_CHANGED §4): *survive* — abstract's "outperforms Adam on regression" (Sven 2nd vs Adam 8th toy, 2nd vs 10th poly, 3rd vs 8th MNIST-LR, 4th vs 9th MNIST-CE), "competitive with L-BFGS at a fraction of the wall time" (L-BFGS 10th–11th, 204/225 polynomial runs diverge), nanoGPT order, P/N direction (1st/2nd at 12 of 19 points). *Must change* — REBUTTALS §1 CIFAR parity; "outperforms standard first-order methods" (MuonW beats Sven on both MNIST + both CIFAR, HIG on toy/poly; **Sven is never 1st on any headline scan**); every polynomial number (additive cubic); REBUTTALS §4.6 "consistently 2nd-fastest" + 1e-8–1e-11 train losses; Fig-5 masking numbers.

**Deviations / open issues.** WHAT_CHANGED is ~1.6 k words (≈2 pp with tables). Swept-study ranks use final val loss, not the rebuttal's time-to-target — stated in the notebook and doc. My rank tables also expose the `fewest-diverged` tier's cost (fresh MNIST-LR SOAP 13/13 at 0.571 because its better-mean config diverges) — worth a sentence in the paper. CIFAR-CE grew 762→765 runs mid-session; **every CIFAR-CE cell recomputes**, so re-run after `tools/select_best.py`.

**make_plots.sh:** uncomment the existing placeholder line `# WP5 legacy diff: legacy_vs_fresh` → `legacy_vs_fresh` in `NEW=()`.