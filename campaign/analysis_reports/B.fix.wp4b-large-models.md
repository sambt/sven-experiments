All eight findings verified against the raw records and files; all eight were real and are fixed. Nothing committed; no shared helper, `make_plots.sh` or `analysis/tables/` entry touched (`git diff HEAD` on all of them is empty — the `tables/` and `corrections.csv` changes in the tree are other WPs').

**Files changed (WP4b-owned only):** `/n/home11/sambt/iaifi/sv3/analysis/large_figs.py`, `/n/home11/sambt/iaifi/sv3/tests/test_large_figs.py` (30 → 40 tests), `/n/home11/sambt/iaifi/sv3/analysis/cifar_analysis.ipynb`, `/n/home11/sambt/iaifi/sv3/analysis/nanogpt_analysis.ipynb`, `/n/home11/sambt/iaifi/sv3/analysis/gpt2_analysis.ipynb`.

| notebook | cells (code) | error cells | runtime |
|---|---|---|---|
| `cifar_analysis` | 28 (16) | **0** | ~150 s |
| `nanogpt_analysis` | 18 (10) | **0** | ~35 s |
| `gpt2_analysis` | 13 (7) | **0** | ~30 s |

`pytest tests/test_large_figs.py` → 40 passed; full `tests/` → **1187 passed, 33 skipped** (154 s). 0 `[style]`/AVERAGED lines.

### Fixes
1. **high, Fig-5 confound (real).** Recount confirms all 15 runs are one config — k=64, lr=1.0, rtol=1e-3, `label_regression` — and the selection of record for the headline label-reg scan is k=128, lr=0.5. New `paramfrac_config` / `headline_sven_config` / `divergence_lrs`; cell 22 markdown + cells 23/27 now print the config, the objective, the diff against the selection ("NOT the headline-selected configuration", EXPERIMENTS.md §3.3, pre-Gram "SET POINT, STILL TENTATIVE"), that lr was never re-tuned per f, and that **both** blow-ups plus **5 of the 5** headline-grid Sven divergences sit at that same lr. The collapse line now reads "**What this bounds:** this configuration at lr=1 … not by itself a measurement of masking at fraction f", and states which single conclusion the confound does not touch (the cost one).
2. **high, GPT-2 GPU-hour share (real).** Full census of 24,850 records = **1,541 GPU-h**; GPT-2 is **124.2 h = 8.1%**, not a third (mnist_scan_labelRegression 348.6 h alone exceeds it). Claim dropped from cell 0; cell 3 now computes `124.2 GPU-h for 29 runs = 4.28 GPU-h per run (range 2.89–9.33 h)`.
3. **medium, timing vs confirm seeds (real).** timing = 4000–4004 / 5000–5004, confirm = 4100–4104 / 5100–5104; Sven CIFAR-CE final val 1.3995 (timing) vs 1.4239 (confirm) vs 1.4028 (tuning). New `seed_note`; cifar cell 8 and nanogpt cell 6 rewritten, both figures now name pass **and** seed set. Aside fixed: the time-axis figure is now 2×2 (val, test, val_acc, test_acc), matching the per-epoch figure.
4. **medium, landscape partial cells (real).** All 9 CIFAR-CE extension cells are partial (3–4 of 5) yet `eligible`, so none carried a mark. `plot_sven_landscape` now returns `(im, mat, ineligible, partial)`, annotates `f/a` under the value of any cell with fewer runs than seeds, and keeps `*` + dashed for ineligible. The 1.35 (3/5) and 1.37 (4/5) cells that undercut the ringed 1.40 are now labelled. New test with a 3-of-5 eligible fixture cell.
5. **medium, robustness denominator (real).** CIFAR-CE Sven: 195 attempted (manifest) vs 185 recorded, 10 never started. New `grid_divergence_counts`; cells 16/27 and nanogpt cell 10 now read "0 of 185 runs … that have a record (0.0%); 10 of the 195 intended have not started yet".
6. **medium, distinct-trajectory claim (real).** distinct_frac is 0.938 for CIFAR-CE Sven (was 0.897). New `distinct_trajectory_check`: **0** colliding `losses.val` curves among 185 runs (39 configs × 5 seeds); label-reg 0 among 90. Cell 18 markdown no longer asserts the claim; cell 21 prints both readings and calls distinct_frac < 1 a ragged-grid artefact of `budget.trajectory_table`'s (seed, lr) grouping. `budget.py` untouched.
7. **medium, v2 profile step-time overlay (real).** v2 masked 530–626 ms vs unmasked 846 ms (masking looks faster); records 342–389 vs 178 ms (masking 1.9–2.2× slower). `plot_paramfrac_cost` now gates the step-time overlay on `profile_step_times`, gated in the notebook on `results_root() == ROOT_V3`; on v2 the panel shows records only with an **on-axes** note. Memory overlay kept (agrees to 0.06%) and restricted to the mask modes the scan ran.
8. **medium, Fig-5 counts + objective (real).** `plot_paramfrac_quality` now draws reduced-n points hollow and annotates "2/3 seeds"; both Fig-5 figures carry a computed subtitle "CIFAR-10 / ResNet18, Sven, label regression (one-hot MSE), k=64, lr=1, rtol=0.001, κ=2, 3 seeds (one fixed configuration, not re-tuned per f)".
9. **medium, `curve_axis` double-prepend (real, reproduced).** `train_eval_acc` vs time gave `[0, 0, 62.879, …]` ending 1182.24 s where `val` ends 1244.38 s. Branch deleted; new `PRE_TRAINING_ALIAS` routes `train_eval_acc` through `train_eval` so `epoch_axis`'s length check *and* its key fallback are both right (a truncated curve too). Test pins both twins equal to the `val` axis on `epoch` and `time`.

### Key numbers the figures/tables now show
**CIFAR label-reg** — Sven 9/11 val, 9/11 test acc; val 0.4838 ± 0.0105, test acc **69.4%** vs 45.6–77.9 (SOAP 77.9, MuonW 76.3); train_eval **0.0674** vs median 0.119 → generalisation failure. **CE** — 9/11 val, **11/11** test acc; 1.424 ± 0.0126, acc **53.0%** vs 56.8–77.9 (SGDm 56.8 / PolyakSGD 63.2 weak, other eight 68.6–77.9); train_eval **0.819** vs median 0.096 → under-optimisation. Cost both scans: **62.9 / 63.0 s/epoch = 22.9× / 23.3× SGD**, 177 ms/step, **22,965 MB = 57×** the leanest. Diag: mean rank inverted 112.5/128 (87.9%) label-reg, 123.3/128 (96.3%) CE → `k` binds. Robustness: **5/90** label-reg (all lr 1.0, rtol 1e-4), **0/185** CE. **Fig 5**: f=1 val 0.668 (3 seeds spanning 0.450–0.854) / acc 74.1%; f=0.5 0.767 / 72.9%; f≤0.25 collapses to 10.0–17.8% vs 10% chance on 2/3, 2/3, 3/3 seeds, 2 diverged; **178.3 ms/step unmasked vs 342–389 masked (1.9–2.2× slower)** at 0.51× memory. **nanoGPT**: Sven 2/5 val, 1.715 ± 0.008 (ppl 5.56) vs AdamW 1.713, paired **+0.0022, CI [−0.023, +0.027]** = tie; wins over Muon/MuonW/SOAP; 2.40 s/epoch, 615 MB; timing pass bit-exact (max_rel_dev 0); 0/20 diverged. **GPT-2**: Sven val **5.198 (ppl 181)** / test 5.107 vs Muon 3.741, MuonW 3.846, AdamW 3.925, SOAP 3.935 → **+39%, last of 5**; 9.25 h/run (3.20×), 2517 ms/step, 36,050 MB; lr optimum **interior** (0.1 of 0.02–1.0) → k=B=16 is the untuned knob.

**Deviations / rejections:** none rejected. `plot_sven_landscape`'s return arity changed 3 → 4 (my module, tests and notebook updated). `paramfrac_table` gained `{metric}_max`.

**Open issues:** CIFAR-CE is still landing (775 records, 10 of 195 Sven runs unstarted, 5 live claims); the extension's 1.35 (3/5) and 1.37 (4/5) cells still beat the pick 1.403, so after `tools/select_best.py` (both groups) re-run `cifar_analysis` only. No prose names a configuration. `profile_results_v3` is still empty — the step-time overlay switches itself on when `results_root()` returns v3.

**make_plots.sh (orchestrator):** uncomment `gpt2_analysis` in `NEW=()`; `cifar_analysis` and `nanogpt_analysis` are already in `STUDIES`.