Both suites green (sv3 `728 passed, 29 skipped`; sven `198 passed, 7 skipped`). Gate 1 complete.

# Gate 1 — report

**Final green run.** Snapshot `/n/holystore01/LABS/iaifi_lab/Users/sambt/sv3_campaign_scratch/smoke_deploy/74987054_203a4e61` — sv3 `749870540208e4098b466dbc0ed6052f37f9bb66`, sven `203a4e612122b430d2eb6afd224d00d6546e7506`, `git_dirty false`. **Temp root: `/n/holystore01/LABS/iaifi_lab/Users/sambt/sv3_campaign_scratch/smoke_74987054`** (216 MB, 100 markers = 97 plan runs + 2 re-epoch + 1 polyak). Also `…/smoke_race_74987054` (18), `…/smoke_f29298c0` (earlier green round). **Real root `sven_experiments` still has 0 entries; legacy dir untouched (mtime 16:51).**

**Jobs** (all COMPLETED 0:0, **0 failed runner processes**). Final matrix `47078906` mig 9:56 · `47078915` 2:02 · `47078925` 13:13 · `47078936` 0:48. (h) `47079587/89/91/92`. (j) race `47079647/48`. (i)+polyak `47079957/60`. Aborted first attempt `47076783/88/94/97` (bug 2). Earlier green round `47077215/20/27/30`. ≈1.6 A100-equivalent GPU-h of 10.

| | verdict | evidence |
|---|---|---|
|a|**FAIL**|97/97 schema-2 records, 0 missing, 0 unexpected, 0 left in `started/`+`claims/`, 85 ok + 12 diverged, all markers match hash8+status. 10 of 13 deliberate points diverged (svd ×4 @step 2, AdamW 3, MuonW 3, SGDm 2, LBFGS 26, JD 2, HIG 2). **2 unintended:** KFAC on both MNIST scans dies with cusolver `linalg.eigh … failed to converge` (CE was learning: val 2.31→1.90, then died in epoch 2). Lifecycle is right (partial curves, `diverged`, no `diverged_at_step` because it isn't a `DivergedError`); the **baseline** is the defect|
|b|PASS|85 ok records, 0 problems. val/test/train_eval all length `num_epochs+1` and finite; 40 classification runs all carry `test_acc`+acc curves; `n_train/n_val/n_test/steps_per_epoch/effective_loader_seed/run_hash/n_params`, both SHAs = snapshot's, `git_dirty false`, gpu_name/host/slurm_job_id present. `split_seed` non-null everywhere except Shakespeare (CONTRACTS: None for positional splits)|
|c|PASS|28 Sven npz. `svs` width ∈ {8,32,64,128,150,170} = B or B/microbatch exactly, **0 NaN-padded entries** (full width on every logged step), `utr` same shape, `svs_step` == {s<200} ∪ {s%20==0} exactly, `sv_min_kept/update_norm/resid_norm/sv_noise_floor` present, no legacy `sv_min`|
|d|PASS|Δval/Δtest: MLP 3.9e-9/5.8e-10, 6.9e-9/1.4e-9, 3.2e-4/3.8e-4 (**rel 7.3e-9** at a recorded loss of 4.3e4); CIFAR Muon 8.1e-6/1.5e-5 (tol 1e-4). `log` ladder = [0,1,2,4,…,256,**312**,512,**624**]|
|e|PASS|16 (scan,seed) groups, up to 13 optimizers per seed; toy 1000→295483371, 1001→1721468797|
|f|PASS|headline CIFAR Sven **191.4 ms/step** (189.2 labelReg, 193.5 CE) vs probe 186.7 → allocator settings carried through. `num_batches_tracked == 702 == 351×2 steps` for **all 7** CIFAR optimizers incl. Sven and LBFGS(mi2). **Peak GPU memory is recorded nowhere** — see worry 5|
|g|PASS|`reconcile --all`: 15 scans, **0 runs to do, incomplete: none** (3 nanoGPT `stale-hash` before fix 3)|
|h|PASS|identical resubmission: 132 mig item-passes at **4–14 s** (vs 154–190 s CIFAR Sven, 24–64 s MLP first time); after: 97 done/97 jsonl, 0 `_stale`, 0 `attempts`, 0 claims/started, no 2-line jsonl|
|i|PASS|`num_epochs` 2→3 on `toy_1d_paramfrac_scan`: exactly its 2 runs re-ran under new hashes `e33130eb`/`a1279a5d`; old generations moved **whole** (jsonl+marker+ckpt+diag) to `_stale/4e3adf9e/`, `_stale/efd713a7/`; nothing else touched|
|j|PASS|`toy_1d_scan` 29 runs split **21/8** between jobs 47078906(MIG)/47078915(A100); mnist_ce 6/8; mnist_labelReg 12/2; lifetimes overlapped 98 s; 0 duplicate jsonl, no `.N` in `_stale`, `attempts/` empty. Forced 2-job race in an empty root: 18/18, job 2 claimed 0, exit 0|

**Step times (ms/step)** — A100: CIFAR Sven 191.4 (probe 186.7), Fig-5 pf0.25 370.8, SGDm 22.1/Adam 22.5/Polyak 23.1/Muon 29.8/SOAP 55.0/LBFGS 62.0; nanoGPT Sven 62.9 (probe 71.7), AdamW 24.8, Muon 34.0; MNIST Sven 9.8–10.2 at NPROC 6 (probe 11.1@1, 13.0@4). MIG NPROC 2–3: toy Adam 2.2, MNIST Adam 1.9, LBFGS 3.3, SOAP 3.5, JD 38–40, **HIG 81–86, Shampoo 131–134**.

**Bugs fixed** (sv3 `robustness-campaign`): `24fb08c` deploy — `git_dirty` described the live worktree, so every record would claim uncommitted code. `f29298c` **launcher — `scheduler=claims` is not in any config's struct: 24/24 runner processes exited 1 having trained nothing**; now `++`, and the dry run composes the pool's own override. `d968cc2` reconcile — LM hash missed `run_grid`'s `vocab_size` injection → a finished nanoGPT scan said "work remains" for ever (a `--chain` job would renew on it for ever). `7498705` pool exports `PYTORCH_ALLOC_CONF` too. `119f802` Gate-1 artifacts.

**Worries before ~16,000 runs.** (1) KFAC/MNIST eigh failure is deterministic — 160 MLP KFAC runs affected; decide: accept, raise `kfac_damping` (3e-3), or drop. (2) Shampoo 35 min/run and HIG 22 min/run at 20 epochs ⇒ ~46 and ~110 process-h beyond the §4.3 line items. (3) nanoGPT `checkpoints: epochs` = 51 states × 3.3 MB ≈ **170 MB/run ≈ 24 GB** for 140 runs — same as `log`, which the config chose it to avoid. (4) Masked Sven is **1.9× slower**, not faster. (5) **No GPU peak-memory column** — add `torch.cuda.max_memory_reserved()` before launch or an allocator/NPROC regression is invisible. (6) PolyakSGD self-limits: `f_star=-1e5` at `max_lr` 1e3 **and** 1e6 both finished `ok` (val 4.3e4, 5.4e28) — no polyak `diverged` record exists from a GPU run (same loop as the 5 that did). (7) **`sv3_deploy/` still holds pre-fix `24fb08c7_203a4e61`** — re-deploy at HEAD into the production base (my snapshot's `experiment_results` points at the smoke root). (8) `WORKER_RESERVE_S=120` reached the pools via `sbatch --export=ALL` — the submitting shell's whole environment reaches every runner. (9) Untested on GPU here: SGD, RMSprop, AdamW/MuonW/RMSprop on CIFAR, and every `bn_mode=frozen` path. (10) gpu_test had 1 drained + 1 down node at the end — the MIG lane may not deliver 2×4 slices tonight.