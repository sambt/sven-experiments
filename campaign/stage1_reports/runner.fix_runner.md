**FIXER — done.** Changed: `experiments/experiment_code/{generic_scan.py,grid.py}`, `tests/{test_runner_training.py,test_runner_lifecycle.py,test_grid.py}`. Untouched: `experiment_utils.py` (only imports `_finish_losses`), `claims/checkpointing/sampler/optim_factory`, `hig.py`, configs, tools, analysis, `sven/` (both repos otherwise clean).

## Fixed
**F1 HIG/BN (high) — confirmed by measurement** (SmallResNet, 4 steps, float64: HIG `num_batches_tracked={0}`, `max|running_mean|=0.528`; SGD `{4}`, `0.319` — double-rate stats + inconsistent `state_dict`). `hig.py` is another track's, so the stop-gap is in `execute`: with norm-stat modules and `bn_mode=batch`, the hig family raises → loud `error` record naming the fix (both forwards under `no_norm_stat_updates` + one explicit `no_grad` train-mode forward + accept `bn_mode`).
**F2 silent per-family default** — `execute` also raises when `ctx.settings["bn_mode"] is None` and `norm_stat_modules(model)` is non-empty. Norm-free configs (MLP/toy/poly/nanoGPT) byte-identical; every CIFAR config already sets a key.
**F5 reconcile LM hash** — new `grid.inject_dataset_facts(model_cfg, vocab_size=, block_size=)`, called by `run_grid`; `_save_resolved_config`'s docstring states it is the post-mutation config reconcile must read.
**F6/F12 unbounded retries** — new `{scan}/attempts/{run_id}.{hash8}.{status}.{token}` (`_record_attempt`), counted by `_failed_attempts` (one glob per executing run); ≥3 → outcome `poisoned`, not retried, with a final `[poisoned]` line. Records/markers stay.
**F9 init checkpoint collision** — `_ScanContext.init_state_name` = `init_mseed{seed}.{grid.model_generation(rcfg)}.pt`; path recorded as `ckpt_init_file`.
**F10 flush policy** — new `_rewrite_each_epoch(policy, num_epochs, n_params)`: accumulating policies (`epochs`/`log`, >1 epoch) always flush per epoch (nanoGPT), `none` never, large models still do under `final`.
**F11 skipped work + exit 0** — the per-run body is now `attempt(spec)`; runs claimed elsewhere are retried in one mop-up pass with a fresh index, and leftovers print `[incomplete] N …` (exit code unchanged per CONTRACTS).
**F13** `torch.cuda.empty_cache()` in the `except` for every family (svd included); `compiler.reset()` stays svd-only in `finally`.
**F14 curve schema** — `except` calls `_finish_losses(losses, t0)` unless already finished, so ok/diverged/oom/error all carry `total_time`/`avg_*`/C-E5 (nan when nothing trained).
**F16** temp name `…tmp.{pid}.{uuid8}`.
**F7/F15 (report defect)** — re-measured with hydra `defaults:` composed: **255 of 276 (config, mode) pairs identical, 21 differing** (F7's numbers; F15's "264+12" comes from an uncomposed loader). The 21 are `cifar10_resnet_ce_scan(+_timing)`, `cifar10_resnet_scan_labelRegression(+_timing)`, `profile_cifar`, `rebuttal_fig5_cifar_paramfrac_scan` (svd gains `_gram_bnbatch`) and `exp_finetune_cifar_smallN` (90 baselines gain `_bnfrozen`). All intended; the oracle is the golden file (90 on-disk `_gram_bnbatch` ids), not HEAD's grid.py. `stop_on_nonfinite` is `True` (grid.py), matching CONTRACTS — part A's deviation 2 is void.

## Rejected
**F4** (normalise hashed dataset/model): dropping `None` keys is unsound (`instantiate` passes an explicit `null`; two different runs would collide) and `sort_keys` already canonicalises order recursively. Documented the freeze rule in `run_hash`'s docstring; CONTRACTS wording + `reconcile --hash-diff` belong to the orchestrator/launcher. **F12b** widening `_DIVERGED_MESSAGE_TOKENS`: cholesky/"not positive definite" raise `torch.linalg.LinAlgError`, already `diverged` via the isinstance branch that precedes message tests; bare `"singular"` would swallow code bugs. **F16b** tolerating `FileNotFoundError` from `os.replace`: the shared temp name was the only way to reach it. **F8** is the configs track's (arithmetic below).

## Tests (observed)
New: `test_every_family_updates_the_running_statistics_once_per_step` (svd gram full+chunked, SGD, LBFGS `max_iter=3`, JD → `num_batches_tracked=={4}` and stats actually moved), `test_hig_is_refused_on_a_batch_statistics_norm_model`, `test_frozen_mode_never_changes_a_buffer` (5 families, HIG included), `test_a_norm_stat_model_needs_an_explicit_bn_mode`, `test_the_shared_init_checkpoint_is_named_per_model_generation` (21 vs 41-param inits coexist), `test_the_epoch_rewrite_policy_follows_the_policy_not_only_the_size`; lifecycle: `test_a_deterministic_failure_stops_being_retried_after_three_attempts`, `test_a_mid_training_failure_keeps_the_curve_keys_of_a_finished_run`, `test_a_run_held_during_the_walk_is_picked_up_by_the_mop_up_pass`, `test_the_saved_config_reproduces_the_records_run_hash`; grid: `test_the_runners_model_mutation_is_a_named_function`, `test_only_the_bn_mode_configs_carry_a_bn_token` (×7), `test_a_bn_token_only_appears_where_a_config_asks_for_a_policy`. Updated `test_execute_keeps_the_per_family_asymmetries` (empty_cache is no longer an asymmetry) and the "losses == {}" assertion. **`167 passed in 29.00s`** (three files); **full suite `723 passed, 29 skipped, 27 warnings in 137.18s`** (`slurm_logs/devtests/devtest-tqeb9w.out`). No golden re-freeze needed (no run_id moved).
**Smoke** (same job, `devtest-tqeb9w.out`, plus `devtest-5crv8G.out`): toy `mode=all`, 8 runs/6 families → pass 1 `ok 8`, pass 2 `skipped 8`, pass 3 two concurrent workers at `num_epochs=3` → `retired 8`, 8 new records, one worker logged `[mop-up] 4 … second pass` + `[incomplete] 1`, both exit 0; `claims`/`started` empty, `attempts` 0, 8 ckpt/diag/done. **Real root safe:** absolute `SV3_RESULTS_ROOT=/n/home/anon/tmp/fixer_smoke/results`, cwd outside the repo, `hydra.run.dir` pinned there, `find` shows no `experiment_results` under the workdir, repo symlink target still 0 entries; every test passes an explicit `tmp_path` `scan_dir` and never calls `scan()`.

## Deviations / gaps
New layout entry `attempts/` (not in CONTRACTS §Layout — orchestrator please add); record gains `ckpt_init_file`; `[incomplete]` fires routinely when siblings are still working (informational, exit 0); one mop-up pass only; `_MAX_FAILED_ATTEMPTS=3` is per hash, cleared by deleting `attempts/` files. Earlier deviations stand.

## Notes
**configs:** set `train_eval_size` — measured: Shakespeare train ≈6971 blocks of 128 tokens (val/test ≈871), so the 10,000 default evaluates the whole train split (≈8.7k block-forwards/epoch vs 6971 trained) → use ~1000; MNIST 10k+10k+10k vs 50k, CIFAR 5k+10k+10k vs 45k → ~2000. Any BN model now **requires** an explicit `bn_mode` (error otherwise), and HIG on a BN model requires `frozen` until `hig.py` is fixed — so `p3_cifar_jd_hig` cannot be enabled as-is. **optimizers track:** fix `HIGWrapper` per F1; then drop the guard's second branch. **launcher:** `reconcile.py` should read `{scan}/configs/{job}.yaml` (or call `grid.inject_dataset_facts`) for hash8; it can now report `attempts/` as "permanently failing", and `[incomplete]`/`poisoned` lines in pool logs are the signal to resubmit/inspect. **analysis:** new columns `ckpt_init_file`; failed records now always carry `total_time`/`avg_*` (nan) plus `val_*` summaries when a val curve exists; `status` still needed to filter `poisoned`-ish `error`/`oom` rows.