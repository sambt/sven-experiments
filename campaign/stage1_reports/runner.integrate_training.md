All work is complete and verified. Final report:

**INTEGRATOR part A — done.** Files changed: `experiments/experiment_code/{generic_scan.py,grid.py,experiment_utils.py,__init__.py}`, `experiments/optimizers/__init__.py`, `pyproject.toml`, `tests/test_grid.py`; created `tests/test_runner_training.py`.

## What changed, per change ID
**Setup (1).** `experiment_utils.py`: the `_CUSTOM_OPTIMIZERS`…`build_standard_optimizer` region (lines 995-1136) is deleted and re-exported `from .optim_factory import (_CUSTOM_OPTIMIZERS, _DEFAULT_WEIGHT_DECAY, _KFACOptimizer, _CombinedOptimizer, _REMOVED_OPTIMIZERS, build_standard_optimizer, get_muon_variant, resolve_weight_decay)`; dropped the `Lion/ScheduleFree*/SOAP` imports. `experiment_code/__init__.py` exports `scan`/`execute`/`set_seed` via PEP-562 `__getattr__` (torch-free `import …grid`). `optimizers/__init__.py` exports `MuonConv`. `pyproject.toml`: `[tool.pytest.ini_options] pythonpath = ["."]` only.

**C-E1/C-E3.** New `_ScanContext.loaders() -> _RunLoaders(train,val,test,train_eval,sampler)`, `_eval_loader`, `_train_eval_dataset` (`subsample_indices(n_train, min(n_train,train_eval_size), dataset.split_seed)`, built once per scan, shared by every optimizer). All four loops get `test_loader`/`train_eval_loader` and **`ctx.loss_fn_svd` (per-sample) — including `train_loop_standard`/LBFGS/Polyak**; `loss_fn_standard` is now unused (comment explains why). `_scan_facts` adds `n_test`, `split_seed`; `_run_facts()` adds `steps_per_epoch`, `num_epochs`, `eval_batch_size`, `train_eval_size`, `eval_every_steps`, `effective_loader_seed`; record gets `summarize_curves` output + `test`/`test_acc`/`train_eval_final`.

**C-S1/S2/S3.** `set_seed(seed_for_run(model_seed, run_id))` in `execute` after `load_state_dict`, before wrapper/optimizer. `EpochPermutationSampler.for_run(..., drop_last=True)` as `batch_sampler` for every family; loops never call `set_epoch`, so new **`_EpochLoader`** advances one epoch per `__iter__` (len/attrs pass through). `run_id` keeps `_lseed{base}`.

**C-E2.** `grid.resolve_bn_mode` / `default_bn_mode` / `bn_mode_suffix` / `BN_MODES`; `bn_mode` in every family's `record_extra` (hashed); `ctx.bn_mode(family)` feeds the wrappers (`bn_mode=`, not `freeze_norm_stats=`) and all four loops; `resolve_svd_settings` keeps `gram_freeze_norm_stats` as the mirrored alias and raises on batch+`hooks`.

**C-L1/C-L2.** `grid.resolve_spectra_schedule` (honours legacy `svd_spectra_every`), `ctx.log_schedule(family)` → `step < dense_first or step % every == 0`, svd only; `empty_cache=ctx.empty_cache` on both Sven ctors. `_split_diagnostics` rewritten: `svs_step = svd_info['step']`, `svs`/`utr` (width B) + `update_norm/resid_norm/sv_min_kept/sv_noise_floor/sv_max/sv_min_all` all indexed by it, new `_epoch_mean_at_steps`, `svd_summary['schedule']`, dense `num_nonzero_svs`, **no `sv_min`**. `_DIAG_LOSS_KEYS` lost `val_batch`/`batch_times_val`.

**C-L3.** Per-run `Checkpointer(scan_dir/ckpt/{run_id}.pt, ctx.checkpoint_policy(family), len(train), num_epochs, rewrite_each_epoch=n_params>1e6)`; `save_init_state` under `final`; `flush()` before `_write_run` and again in `finally`; `ckpt_error` recorded.

**Records.** `schema_version=2`, `actual_param_fraction` (svd), `muon_variant` (runtime) + `muon_rule`=`grid.MUON_RULE_TOKEN` in `record_extra` (Muon/MuonW only), `bn_mode`, `checkpoint_policy`, `svd_spectra_schedule`. `losses={}` bound before the `try`, `stop_on_nonfinite` default **False**. Also: `output_dir = os.environ.get("SV3_RESULTS_ROOT") or "experiment_results"` (one line; tell agent B).

## Tests
`tests/test_runner_training.py` (18 tests, float64, tiny in-memory 3-split dataset, `scan_dir` in `tmp_path`): **`18 passed in 11.03s`**. `tests/test_grid.py` +4 tests, `LEGACY_RECORD_KEYS + SCHEMA2_RECORD_KEYS`, asymmetry test updated (drop_last moved to `loaders`, `empty_cache=ctx.empty_cache`×2, flush order, `losses=losses`): **`116 passed in 6.24s`**. Whole suite **`1 failed, 664 passed, 29 skipped in 54.50s`** — the failure is `tests/test_tools_worker_pool.py::test_the_scheduler_override_is_default_on_and_can_be_dropped`, the launcher track's in-flight file. sven: **`198 passed, 7 skipped in 6.68s`**.
Acceptance coverage: val==example-weighted mean (`test_recorded_val_is_the_example_weighted_mean`, ragged 16/16/8); test columns finite (`test_test_columns_are_on_the_record_and_finite`); checkpoint reload reproduces final val/test to 1e-6 (`test_the_final_checkpoint_reproduces_the_recorded_val_and_test`); C-S1 pf=0.5 bit-identical alone vs after another run (`test_a_masked_sven_run_is_position_independent`, curves+`svs`); same/different batches (`test_every_optimizer_of_a_model_seed_sees_the_same_batches`, +7-batch tail drop); spectra width B + schedule (`test_spectra_are_stored_with_their_own_step_index`).
**Smoke:** 1-epoch `mode=all` toy scan, 7 runs over all six families, pass 2 skipped all 7, npz `svs (4,8)`, `svs_step [0,1,3,6]`, ckpt steps `[0,1,2,4,8]`, `muon_variant hidden2d+adamw:match_rms_adamw:v1`. **Real root untouched:** absolute `SV3_RESULTS_ROOT` under `/n/home11/sambt/tmp/runner_smoke`, cwd outside the repo, hydra dir pinned there, `find` shows no `experiment_results` under the workdir, repo symlink target still 0 entries (it is empty per §4.1). Unit tests use `tmp_path` and never call `scan()`.
**Run-id neutrality proved:** scratch script compared HEAD's `grid.py` with mine over HEAD's configs — **264 (config, mode) pairs, 0 differ**.

## Deviations
1. `bn_mode`'s default is **per family** (`frozen` for svd+gram, else `batch`) rather than a flat `batch`: a flat default would rename every gram run_id and turn every `gram_capture: hooks` scan (MNIST, poly, toy, nanoGPT, fine-tune) into a hard raise. An explicit `bn_mode` applies to all families; `_bnfrozen` marks only a deviation.
2. `stop_on_nonfinite` defaults to False (per task, vs CONTRACTS' `true`).
3. New npz name `sv_min_all` (σ_M) since `sv_min` is forbidden (F20); `svd_summary` has `schedule`/`sv_min_all_epoch`, not `spectra_every`/`sv_min_epoch`.
4. Extra record keys beyond the list: `train_eval_size`, `eval_every_steps`, `num_epochs`, `train_eval_final`, `ckpt_error`.
5. `SV3_RESULTS_ROOT` implemented here.

## Gaps / notes
* No failure record, `status`, `diverged_at_step`, claims/dedup/manifest/provenance yet (agent B); `execute` still prints and continues. Classify `DivergedError` **first**; `err.losses` holds finished partial curves.
* **configs:** no scan sets `train_eval_size` — nanoGPT then evaluates 10,000 blocks (~10M tokens) per epoch end against 200 val/test blocks; set ~200-1000. `eval_batch_size: 256` sequences × 1024 tokens may OOM (legacy val used 64). CIFAR: val+test+train_eval = 25k images/epoch at `eval_batch_size 2048`. `bn_mode: batch` requires `gram_capture: chunked|full` (raises otherwise). Muon/MuonW lr grids still need the `match_rms_adamw` shift.
* **analysis:** new records have `sv_max`, `sv_min_all`, `sv_min_kept`, `sv_noise_floor`, `update_norm`, `resid_norm`, `utr` all on `svs_step`; no `sv_min`/`val_batch`/`batch_times_val`; `avg_batch_time_val` gone (use `avg_train_time`); outcome columns `test`, `test_acc`, `actual_param_fraction`, `muon_variant`, `val_best`, `val_last3_mean`, `train_eval_final` (never select on `test`).
* I did one read-modify-write of `tests/test_grid.py` while the configs track was editing it; both edit sets are present and 116 tests pass, but they should re-check their `INVENTORY`/`EXPLAINED` rows.