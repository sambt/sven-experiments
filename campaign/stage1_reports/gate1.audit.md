VERDICT: GO

**Independently reproduced** (own scripts under `/tmp/claude-66176/.../scratchpad/audit/`, copies in `/n/home/anon/tmp/claude_audit_30b30b16/`; torch work via `run_cpu_tests.sh`).

| check | my result |
|---|---|
| (1) coverage | **98** records (not 97), **86 ok / 12 diverged**, all schema 2, 98 markers matching `run_id.hash8.status`, 0 in `claims/`+`started/`+`attempts/`, 0 multi-line jsonl, 98 unique run_ids. Every family (svd/standard/lbfgs/polyak/jd/hig) × toy/poly/MNIST-labelReg/MNIST-CE present; CIFAR 7 optimizers; nanoGPT 3. All `val`/`test`/`train_eval` length `num_epochs+1` and finite; 14 CE runs carry finite `test_acc`+curves. 10 deliberate points `diverged` with 1-entry partial curves + `diverged_at_step`; Adam/Muon@lr1e3 and Polyak stayed finite (`ok`, val 4.3e4/5.4e28) |
| (2) recompute | **7** records, 5 families, rebuilt dataset+model, my own per-sample losses and example/token weighting: rel err val/test = 7.9e-9/4.3e-8 (poly HIG), 2.2e-8 (MNIST-CE Adam), 2.2e-9 (MNIST-labelReg LBFGS), 5.5e-6 (toy Sven), 1.1e-5 (CIFAR Adam), 5.5e-5 (CIFAR Sven), 7.9e-9 (nanoGPT AdamW, token-weighted). Accuracies match. **My eval mutated zero buffers**, and CIFAR reproduces only from running stats → eval-mode + side-effect-free confirmed |
| (3) CIFAR BN | `num_batches_tracked == {702} == 351×2` on all 20 BN layers for **all 7** optimizers incl. Sven/LBFGS(mi2); init ckpt = 0. `mean\|Δrunning_mean\|` vs init: Sven 0.601, Adam 0.615, SGDm 0.430, Muon 0.777 — no double-rate signature. Sven **189.0 ms/step median, p10–p90 187.5–190.1** (probe 186.7); Fig-5 pf0.25 351 ms (**masked 1.9× slower**) |
| (4) spectra/order | 32 svd npz: width ∈ {8,32,64,128,150,170} = B or B/microbatch, **0 NaN**, `utr` same shape, `svs_step` == {s<200}∪{s%20==0} recomputed from `steps_per_epoch×num_epochs` — exact on all 32. `batch_indices_for_run` reproduced recorded per-step `train_batch` at 9–13 steps for 4 optimizers (rel ≤3.5e-4) with **byte-identical indices across optimizers** at one model seed |
| (5) provenance | 98/98 SHAs == DEPLOY_INFO (`7498705…`/`203a4e6…`), `git_dirty false`, `git_source deploy_info` |
| (6) roots | real root **0 entries**; legacy has no file newer than 16:52; repo symlink → the empty root; pools wrote only to `smoke_74987054` |
| (7) commits | 4 fixes touch only `tools/`, `tests/`, `campaign/` — nothing in `experiments/`, `sven/`, configs. No check weakened. My runs: sv3 **728 passed, 29 skipped**; sven **198 passed, 7 skipped**. Pool logs: "0 failed runner process(es)" in every job; all jobs COMPLETED 0:0 |

**Report corrections:** 98/86 not 97/85 (extra Polyak `max_lr` point). (g) no longer reproduces — `reconcile --all plan_smoke.yaml` exits **1**, "2 runs to do, stale-hash", because the (i) re-epoch test ran after; `_stale/4e3adf9e|efd713a7/` are correct and complete. One cross-scan duplicate `run_hash` (`polynomial_scan` ≡ `rebuttal_batchsize_polynomial_scan` at B=32,k=32) — harmless (dedup is per-scan) and gave a free bit-identical reproducibility check, but hash tooling must stay scan-scoped.

**Blocker before launch (do not skip):** `sv3_deploy/` holds only `24fb08c7` and `6fd8fd9e`, both **pre-f29298c** — launching from either reproduces "24/24 runners exit 1, nothing trained". Re-run `tools/deploy_snapshot.sh` at HEAD `119f802`, then confirm `experiment_results → …/sven_experiments` and `git_dirty: false`.

**Gate-1 shortfall accepted:** `bn_mode=frozen` never ran on a GPU; only `exp_finetune_cifar_smallN` uses it and it is `enabled: false`/out of the plan. `oom`/`error` statuses are CPU-test-only.

**First hour — watch:** (a) `[pool] results root` on job 1 must read `…/sven_experiments` (`--export=ALL` lets a stray `SV3_RESULTS_ROOT` silently redirect everything); (b) first CIFAR Sven item's ms/step ≈191 — >250 means the allocator env was lost; (c) `[poisoned]` lines and any non-empty `attempts/` (`[incomplete] N` is routine); (d) first `reconcile` after the P0 MLP job: expect 0 `started-only`/`claimed-live`/`stale-hash`; (e) KFAC on both MNIST scans dies deterministically (cusolver `eigh` non-convergence) as terminal `diverged`, never retried, reconcile still COMPLETE — so KFAC will have **no MNIST data point**; decide (damping 3e-3 / drop) before the `O_SECOND` items. Confined to MLPs: CIFAR second-order is SOAP-only; (f) Shampoo 131–134 and HIG 81–86 ms/step on MIG ⇒ ~46+110 process-h over §4.3 — watch the 12 h MIG wall; (g) nanoGPT `checkpoints: epochs` ≈170 MB/run ×140 ≈ 24 GB; (h) no peak-memory column, so an NPROC/allocator regression is only visible as step time.