**TRACK grid (C-R2, refactor only) — done.**

## Files created / changed
| file | state |
|---|---|
| `/n/home/anon/sven-experiments/experiments/experiment_code/grid.py` | **new**, 520 lines, stdlib only |
| `/n/home/anon/sven-experiments/experiments/experiment_code/generic_scan.py` | rewritten below `_scan_facts`: `_ScanContext`, `_describe`, `execute(spec, ctx)`, `scan(cfg)`; `_build_id_string` **moved** to `grid.build_id_string` (grep-verified: no other user); `SVD_INFO_MODES` moved to grid.py and re-imported so `generic_scan.SVD_INFO_MODES` still resolves; dropped now-unused `itertools.product`, `process_hparam_config`, `resolve_weight_decay` imports; added `assert set(LOSS_KEYS)==set(SVD_LOSS_FNS)==set(STANDARD_LOSS_FNS)` and the same for `SVD_RESIDUAL_FNS` |
| `/n/home/anon/sven-experiments/tests/test_grid.py` | **new**, 101 tests, 4.1 s, torch-free |
| `/n/home/anon/sven-experiments/tests/golden/*.txt` | 9 dir listings (`ls` only) + 2 **ordered legacy** enumerations (`toy_1d_scan.order.txt`, `rebuttal_batchsize_polynomial_scan.order.txt`), 527 KB total |

## Acceptance tests — `101 passed in 4.11s` (single observed summary line for the whole file)
- **golden ids** (9 scans): every on-disk run_id is produced. 2 explained classes in `rebuttal_batchsize_polynomial_scan`, encoded as asserted rules, not loosened assertions: 120 `_optimAdamW_` ids predate the AdamW/MuonW wd-in-run_id convention (test asserts each equals today's id minus `_wd0.0`); 120 `_optimMuonW_wd0.1_` ids come from `submit_reruns_2026-09-17_part2.sh`'s `weight_decays=[0.1]` override (test asserts they *are* produced under it). Both counts pinned.
- **family counts** == `grid_inventory.md` §1 for all 9 (e.g. mnist_scan_ce svd 640 / standard 180 / lbfgs 135 / polyak 5 / jd 20 / hig 80).
- **order + shard**: `<scan>.order.txt` frozen by executing `scan()` **from git HEAD** (source unmodified; exec'd inside the real package, with `instantiate`/`set_seed`/`_scan_facts` stubbed and an `os` shim whose `path.exists` records the run_id and returns True). `scratchpad/legacy_equiv.py` proved *ordered* identity for 11 config/mode combos **and** `shard(specs,n,i) == legacy` for (1,0),(2,0),(2,1),(3,2),(6,4),(7,0): "ALL EQUIVALENT". The permanent test compares against the frozen files plus the modulo-counter identity.
- **disjoint/cover** for n_shards 1,2,3,4,6; **seed-major** order; **run_id uniqueness**; **record key order** == the six legacy `result` dicts; svd id details (k_values vs k_fractions, `use_gram` svd_mode collapse with a 3-mode list not multiplying the grid, `_mb/_pf[_mask]/_gram/_bnbatch/_kappa/_lossbrier`); wd grid filter; mode flags incl. `has_torchjd=False` dropping JD; `run_hash` (64 hex, unique per point, moves on num_epochs/loss/dataset/model/data_seed/eval_batch_size, stable under `svd_spectra_every`); **all 37 configs** expand with unique ids+hashes (5 `*_timing` inheriting configs included, `profile_*` skipped by `name != "scan"`); **torch-free import** asserted in a subprocess (`torch/hydra/sven/omegaconf/numpy/pandas` absent, exec < 1 s); **copied-helper drift guard** (AST-compares `listify`/`resolve_weight_decay`/`process_hparam_config`/`_DEFAULT_WEIGHT_DECAY` against `experiment_utils.py`, which another agent is editing).
- **smoke run** via `campaign/run_cpu_tests.sh -C /n/home/anon/tmp/grid_smoke bash smoke.sh` (script kept there; log `slurm_logs/devtests/devtest-rNh2l2.out`): 1 epoch, `mode=all`, all six families wrote jsonl+npz; pass 2 printed `[skip]` for all 6; `+n_shards=2 +shard_id=1` ran exactly indices 1,3,5 (Adam, PolyakSGD, HIG). **Real results root untouched**: the script `cd`s into `~/tmp/grid_smoke/work`, which contains its own empty `experiment_results/`, and pins `hydra.run.dir` under that temp tree — the hard-coded relative `"experiment_results"` therefore cannot resolve to the repo symlink; verified by re-counting `experiment_results/toy_1d_scan/*.jsonl` = 734 before and after.

## Deviations from CONTRACTS.md
1. `RunSpec` carries **no `run_hash` field** (the scout's proposal); `run_hash(spec, rcfg)` is a function, as the Interfaces section specifies, so specs stay cheap and hashing stays optional.
2. `expand_grid` gained keyword-only `mode`, `has_torchjd`, `jd_aggregators`, `verbose` (the mode flags the task asked for; `has_torchjd` is required for legacy-identical enumeration).
3. Two **new stdout lines only**: `Grid: N runs; this shard (i/n): M`, and family banners now print per (seed, family) block actually reached rather than unconditionally.
4. `schema_version` is defined (`grid.SCHEMA_VERSION = 2`) and hashed, but **not yet written to records** — that is the integrator's C-R3 work.

## For the integrator
- New API: `grid.expand_grid(rcfg, *, mode=None, has_torchjd=True, jd_aggregators=JD_AGGREGATOR_NAMES, verbose=True) -> list[RunSpec]`; `grid.shard(specs, n_shards=1, shard_id=0)`; `grid.mode_flags(rcfg, mode=None) -> {svd,standard,jd,hig}`; `grid.resolve_scan_settings(rcfg)`; `grid.resolve_svd_settings(rcfg)`; `grid.build_id_string(cfg)`; `grid.run_hash(spec, rcfg, *, schema_version=2)`; `grid.hash8(spec, rcfg)` (also accepts a hash string); `RunSpec(family, run_id, model_seed, loader_seed, batch_size, hparams, record_extra)`; `generic_scan.execute(spec, ctx)`, `generic_scan._ScanContext(cfg, rcfg, dataset, scan_dir, settings, svd_settings)` with `.seed_state(model_seed) -> (init_state, common)` and `.loaders(spec, drop_last=False)`.
- **Hooks you will want**: C-R1 → the single `try/except/finally` in `execute` (pre-bound `optimizer = None` already there); C-R3 → the dedup `os.path.exists` line in `scan`'s driver loop, one place; C-R4 → `_scan_facts`; C-S1/S2 → `execute` right after `load_state_dict`; results root → `output_dir` in `scan` (still hard-coded, one line).
- **`rcfg["model"]` is now refreshed** after the LM vocab/block injection so `run_hash` sees the instantiated model config (scout §3 C-R3 note).
- **Not owned by me, needs a change**: `experiments/experiment_code/__init__.py` eagerly imports `generic_scan`, so `import experiments.experiment_code.grid` pulls torch; a torch-free consumer (`tools/reconcile.py`, launchers) must load `grid.py` by path, as `tests/test_grid.py::_load_grid` does, until those eager imports are dropped.
- **Copies, originals left in place** in `experiment_utils.py`: `listify`, `resolve_weight_decay`, `_DEFAULT_WEIGHT_DECAY`, `process_hparam_config`. Any C-B2/C-X1 edit to the originals (e.g. an `SGDm` registry entry or a new hparam key) must be mirrored into `grid.py` — `test_copied_helpers_match_experiment_utils` fails loudly otherwise.
- Legacy oddity preserved, not fixed: `seeds = rcfg.get("model_seeds")` has no `model_seed` fallback despite the docstring; a config without `model_seeds` still raises `TypeError`.