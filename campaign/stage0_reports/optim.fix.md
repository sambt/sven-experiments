**Files changed** (only track-owned): `/n/home11/sambt/iaifi/sv3/experiments/experiment_code/optim_factory.py`, `/n/home11/sambt/iaifi/sv3/experiments/optimizers/muon_conv.py`, `/n/home11/sambt/iaifi/sv3/tests/test_optim_factory.py`. **Observed: `63 passed in 4.61s`** (also `63 passed in 63.79s` / `132.83s` on a loaded node — test bodies total 1.7 s, the rest is the cold `torch`+`sven` import; my additions cost ~0: `experiment_utils` adds 0.0 s on top of `optim_factory`'s 11.9 s).

**Findings worked through**
1. **high, input layer → AdamW: real, fixed.** Deleted the `if not has_embedding` branch and rule 3; only the *last* projection (head) plus embeddings/1-D/unrecognised go to AdamW, exactly C-B5's wording. Measured: `MLP(784,[32]*3,10)` Muon coverage 7.4% → **98.45%** (w=128: 98.76%); `resnet18_functional` now 20 conv kernels (incl. the stem) → MuonConv, >99% of params, only `fc.weight` is ≥2-D in AdamW; `SmallCNN` conv1 joins MuonConv. No user sign-off needed since the deviation, not the fix, was the unsanctioned part. New tests: `test_muon_actually_covers_the_mnist_headline_model` (frac ≈ 0.9845/0.9876, >0.98, `net.0.weight` in `muon_2d`), rewritten `test_groups_mlp` / `_resnet18` / `_small_convnets`, and `test_single_layer_model_refuses_muon_but_two_layers_are_enough` (1 Linear still raises; `MLP(4,[8],3)` now yields one Muon matrix).
2. **medium, variant cannot distinguish constructions: accepted.** Added `MUON_RULE_VERSION="v1"`; variants are now `hidden2d+adamw:match_rms_adamw:v1` / `hidden2d+convflat+adamw:match_rms_adamw:v1`, plus a model-independent `MUON_RULE_TOKEN="hidden2d+convflat:match_rms_adamw:v1"` for `record_extra`. Test `test_variant_and_rule_token_carry_the_rule_version`.
3. **medium, "legacy = no lr adjustment" is false: confirmed, fixed.** `torch/optim/_muon.py:_adjust_lr` maps `None`→`sqrt(max(1,A/B))`. `MUON_VARIANT_LEGACY` is now `"all2d+adamw:original"` and the comment states the rule. Test `test_legacy_adjust_lr_fn_none_is_the_original_rule` asserts `None` is bit-identical to `"original"` and that the `match_rms_adamw`/legacy step ratio equals `0.2·sqrt(max(A,B))/sqrt(max(1,A/B))` to rtol 1e-9.
4. **medium, copy fidelity untested: accepted.** `test_copied_branches_build_the_same_optimizer_as_experiment_utils` builds Adam/AdamW(wd None,0.1)/SGD/RMSprop/Lion/SOAP/LBFGS/PolyakSGD from both factories and compares type + every `param_groups` entry minus `params`; skips cleanly once `experiment_utils` re-exports (or is mid-edit).
5. **low, MuonW wd hole: fixed.** `_build_muon` now uses `resolve_weight_decay(optim_name, kwargs.get("weight_decay"))`; explicit values unchanged. Test asserts `MuonW` with no kwarg → `[0.1, 0.01]`, `Muon` → `[0.0,0.0]`, explicit 0.0 → `[0.0,0.01]`.
6. **low, tensor lr: fixed.** Copied torch's 1-element check and `_to_scalar(group["lr"])`. Test covers the raise and equality with stock Muon under a tensor lr.
7. **low, head detection: partly accepted.** Rejected the "head out-width == model output width" guard (no model exposes an output width; LM heads are vocab-wide). Instead added `test_groups_resnet18_pretrained`, skipped if the ImageNet checkpoint is not already in `TORCH_HOME` (it is on the cluster; tests never download) — 20 MuonConv kernels, `fc.weight` only in AdamW, so `replace_batchnorm` + head replacement do not disturb registration order.

**Deviations from CONTRACTS.md**: none. `resolve_weight_decay` semantics untouched; no wd grid; only the three owned files.

**Integrator notes**: API as in the impl report, plus `MUON_RULE_VERSION`, `MUON_RULE_TOKEN`; `MUON_VARIANT_*` values changed (they now end `:v1`). (a) `grid.py`: put `muon_rule: MUON_RULE_TOKEN` in `record_extra` for `optim_name in {Muon,MuonW}` so it enters `run_hash` (C-R3 would otherwise skip old-rule runs). (b) `generic_scan.py:450`: record `muon_variant = get_muon_variant(optimizer)`. (c) **lr grids**: `match_rms_adamw` makes Muon's effective step 5.6× legacy on MNIST `net.0` (1.13× on the 32×32 hidden ones), ~2.3–4.5× on nanoGPT, and up to 13.6× for resnet18 `layer4` kernels (never in Muon before) — extend Muon/MuonW `lrs` down by two half-decades or give them their own list in `mnist_scan_*`, `exp_nanogpt_speedrun`, CIFAR headline configs. (d) Still needed in files I do not own: `submit_fresh_suite.sh:49` (`MuonW`, `SGDm`), `analysis/style.py`, `experiments/optimizers/__init__.py` (export `MuonConv`), CIFAR configs (Muon addable under O6).

Note for the orchestrator: the relayed user question about only two review agents running concurrently is a workflow-script setting; nothing inside a track can change it.