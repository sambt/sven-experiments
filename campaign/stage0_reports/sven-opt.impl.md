## TRACK sven-opt — done

**Files changed:** `/n/home11/sambt/iaifi/sv3/sven/sven/opt/sven.py` (+250/−77). **Created:** `/n/home11/sambt/iaifi/sv3/sven/tests/test_torch_logging.py` (18 tests). No other file touched.

### Implementation
New on `Sven` (inherited by both Gram classes): attr `log_this_step=True`, attr `step_count`, ctor kwarg `empty_cache: bool = True`, `_maybe_empty_cache()` (all 4 `torch.cuda.empty_cache()` sites now route through it), `_gram_fp64(J)` (`J Jᵀ` in fp64, accumulated over blocks of ≤`_GRAM_BLOCK_ELEMS`=2²³ Jacobian entries — `J.double()` never materialised), `_spectrum_from_gram(G) -> (sigma, sigma², U)` (fp64 `eigh` + flips, bit-identical to the old inline code), `_sv_min_kept(sigma, filt)`, `_record_rank(kept)`, `_log_step(...)`, `finalize_svd_info() -> dict[str, Any]` (idempotent; numpy arrays; `svd_info` stays readable the old way). `svd_info` gained `step, utr, update_norm, resid_norm, sv_min_kept`; `num_nonzero_svs` still per step.

Sync removal: `SvenGram` replaced `nonzero()` + `keep.max().item()` + `count_nonzero().item()` with one `bool(keep.any())` (the divergence guard, kept) and a boolean mask multiplied into `s_inv_sq` (legal because sigma is descending, so the rtol mask *is* the old prefix). Kept-rank is appended as a 0-d device tensor (converted eagerly only when it is already on CPU, so legacy asserts still read ints) and materialised in `finalize_svd_info()`. Per-step syncs: 4→1 (`SvenGram`), 2→1 (`SvenGramReg`), 2→1 (classic; the survivor is `pinv.py:63`, which I did not touch). `update_norm` is the norm of the applied update **before** the `−lr` scaling; `_update_params` now returns it and `_update_params_variable_k` returns `(k_used, substep_losses, applied)`.

### Acceptance tests (all in `sven/tests/test_torch_logging.py`, fp64/CPU, MLP 4-16-16-3, M=8)
- `test_gram_svs_match_svdvals[hooks,chunked]`, `test_classic_svs_match_svdvals`: `svs` has length M even at `k=4, rtol=0.1`, equals `svdvals(J)` (rtol 1e-9), and `sv_min_kept == svs[n_kept−1] > svs[−1]` (pins F20).
- `test_gram_update_equals_utr_formula[k_full,k_small,rtol_trunc]`, `test_classic_update_equals_utr_formula[8,5]`: applied update == `Jᵀ U[:, :n] (utr[:n]/s[:n]²)`, `n = num_nonzero_svs`; `utr` compared signed against `evecsᵀ r` from the *same* `eigh` input; `update_norm`/`resid_norm` checked.
- `test_log_this_step_false_records_only_rank[Sven,SvenGram]`: params `torch.equal` over 3 steps, all 6 heavy keys empty, `num_nonzero_svs` identical and length 3, `finalize` repeatable.
- `test_empty_cache_flag[Sven-2,SvenGram-1]`: with `torch.cuda.is_available`/`empty_cache` mocked, 2 steps give exactly 4/2 calls when `True`, 0 when `False`, params `torch.equal`.
- Plus `test_variable_k_logs_the_applied_update`, `test_reg_logs_full_spectrum_and_utr`, `test_gram_fp64_blocks_match_one_shot`, `test_track_svd_info_false_records_nothing`, `test_zero_gram_still_raises`.

**Observed:** `18 passed in 16.84s` (new file); `56 passed in 23.74s` (`test_torch_gram.py test_torch_gram_reg.py test_torch_residual.py`); `179 passed, 5 skipped in 7.02s` (whole `sven/tests` via `campaign/run_cpu_tests.sh`; the 5 skips are pre-existing: 4 jax `importorskip`, 1 missing OpenWebText shard).

### Deviations / integrator notes
1. **`SvenGramReg` got the full record** (contract allowed skipping it) — it was ~10 lines once its zero-knob branch had to switch to mask-multiply to stay bit-identical to `SvenGram` (`test_all_knobs_zero_is_bitwise_stock` still passes). Its `utr`/`resid_norm` are the rhs *actually solved*, i.e. `r + jvp_coef·Mθ` when `jvp_coef≠0`; `sv_min_kept` = smallest sigma above the eps floor. Documented in the class docstring.
2. **`SvenGramReg._rows_jvp` now uses `self.model._pass_norm_stats()`** instead of `_frozen_norm_stats()` — required by the C-E2 track (its docstring lists `jvp`). Cross-repo dependency on `sven/sven/nn/sven_wrapper.py:267`.
3. **Call sites I do not own**: `generic_scan.py:576` must become `optimizer.finalize_svd_info()` — on GPU `num_nonzero_svs` holds 0-d CUDA tensors, so `np.asarray(..., dtype=np.int32)` at `generic_scan.py:197` would fail. `_split_diagnostics` (172-226) must index spectra by `svd_info["step"]`, not by position (`svs` is sparse once `log_this_step` is scheduled), and store `utr/update_norm/resid_norm/sv_min_kept`; `sv_min` from `svs[-1]` is now sigma_M, the kept one is `sv_min_kept` (F20 rename). Pass `empty_cache=False` at `generic_scan.py:523/531` and `optimizer_profile.py:195/207` for timing/profile runs.
4. **Classic `svs` changed meaning** (was rtol+k-truncated `1/S_inv`): now the full fp64 spectrum, costing one blocked `J Jᵀ` + `eigh` per *logged* step, only under `track_svd_info=True` (profiler unaffected). Under `variable_k`, `sv_min_kept = sigma[k_used−1]`, `inf` if no component was accepted.
5. `_GRAM_BLOCK_ELEMS` is a class attribute so it can be lowered in tests/low-memory runs.