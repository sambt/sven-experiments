All three medium findings worked through; full sven suite green apart from another track's in-flight file.

**Files changed** (track-owned only): `/n/home11/sambt/iaifi/sv3/sven/sven/nn/sven_wrapper.py`, `/n/home11/sambt/iaifi/sv3/sven/sven/nn/gram_wrapper.py`, `/n/home11/sambt/iaifi/sv3/sven/tests/test_torch_bn_policy.py`, `/n/home11/sambt/iaifi/sv3/sven/tests/test_torch_param_fraction_record.py`. Untouched this round: `sven/pyproject.toml`, `tests/test_jax_*.py` (already done), `sven/sven/opt/*` (other agent).

**M1 `evaluate_and_loss` (kept, documented, needs one ratifying line I cannot write).** Verified the deviation is real but *only* under `bn_mode="frozen"`, where CONTRACTS.md:39 ("train-mode normalisation") collides with CONTRACTS.md:31 ("frozen = eval mode **always**, train and eval"). Under `batch` the implementation *is* literally train-mode-no-write. I kept the capture-following behaviour and wrote the collision + resolution into the `evaluate_and_loss` docstring (`sven_wrapper.py:381-396`) instead of changing it silently. Evidence re-verified: all 9 configs with `variable_k` set it `false`, and `grid.py:242` raises on `use_gram` + `variable_k`, so no campaign run is affected. **Integrator/user: add to CONTRACTS.md Decisions** — "`evaluate_and_loss()` = train-mode-no-write under `bn_mode=batch`; under `frozen` the frozen decision wins (eval-stat normalisation, no write)".

**M2 jvp coverage (fixed).** `test_batch_mode_updates_stats_exactly_once_gram` is now parametrised over `[SvenGram, SvenGramReg]` × `["full","chunked"]`; the reg case passes `fisher_decay=1e-3` so `jvp_coef != 0` and `SvenGramReg._rows_jvp` runs. Added `count_norm_forwards()` (forward-pre-hook counter splitting forwards into writing/suppressed) so the test is not vacuous: it asserts `tracking == n_norm_layers` (exactly one writing forward per layer) and `suppressed >= 3*n_norm` for the reg path (capture + jvp + delta) vs `>= 2*n_norm` otherwise. This pins `sven/opt/sven.py:582`'s `self.model._pass_norm_stats()` against a future edit by the sven-opt track.

**M3 `generic_scan.py:260` (rejected as out of track, confirmed unwired).** Re-grepped at 16:30: `actual_param_fraction` still appears only in `analysis/style.py:444`. The wrapper half is done; the runner half is the runner track's. **Integrator:** in the record assembly, `record['actual_param_fraction'] = train_model.mean_actual_param_fraction` for svd-family runs (safe to read unconditionally — returns `1.0` unmasked).

**Lows also fixed** (cheap, in-track): hooks + `bn_mode="batch"` now raises regardless of the modules' current training flags (`gram_wrapper.py:_check_hooks_supported`), killing the `batch`-labelled/frozen-behaviour mislabel; `_check_batch_stat_norms` now rejects `_InstanceNorm` with running stats (`no_norm_stat_updates` cannot suppress it and clearing the flag changes its normalisation) with a docstring scope note — note the discriminator is `_InstanceNorm`, **not** `not isinstance(_BatchNorm)`, because sv3's vendored BN subclasses `_NormBase` directly; `mean_actual_param_fraction` before the first step now returns the *requested* `param_fraction` (not `1.0`, not NaN — the value goes into jsonl).

**New/changed assertions + observed summary lines**
- `pytest tests/test_torch_bn_policy.py -q` (local) → `24 passed in 101.26s (0:01:41)`
- `pytest tests/test_torch_param_fraction_record.py -q` (local) → `11 passed in 56.49s`
- `run_cpu_tests.sh -C sven … pytest <both owned files> -q` → `35 passed in 3.81s`
- `run_cpu_tests.sh -C sven … pytest -q --ignore=tests/test_torch_logging.py` → `164 passed, 5 skipped in 14.33s`
- bare `pytest -q` → `7 failed, 175 passed, 5 skipped in 20.80s`; **all 7 failures are `tests/test_torch_logging.py` (C-L1 `utr`, factor-4 mismatch), the sven-opt track's in-flight work**: that file and `sven/opt/sven.py` were written at 16:21/16:23, after my last nn edit (16:18), and the file instantiates no norm layer, so the nn changes cannot reach it.

New tests: `test_instancenorm_rejected_on_batch_stat_path` (batch path raises; frozen path runs and writes no buffer) and the eval-mode half of `test_hooks_capture_rejects_batch_mode_norm_layers`. No other deviations from CONTRACTS.md; no new public API beyond the implementer's report.