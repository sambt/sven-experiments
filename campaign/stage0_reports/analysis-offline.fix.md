## Fixer report — track `analysis-offline`

**All four findings confirmed and fixed; none rejected.** Verified 1(a), 2 and 3 by reproduction (scripts in scratchpad), 4 by construction and by the data (e.g. 10 of 734 `toy_1d_scan` runs have `eq_check = NaN`, so a vacuous pass is reachable).

### Changed
- **`analysis/profile_helpers.py`** (F1). New `TIMED = ('ok', 'nonfinite')` beside `STATUS_NOTE`: a `nonfinite` row was timed, so every value-consuming filter now uses `df.status.isin(TIMED)` — `add_relative` (the reference bug), `plot_sweep` (line + `bad`), `plot_pareto` (sweep and ref), `plot_phase_bars`, `plot_steadiness`, `scaling_table`. Flags kept/added: `method_table.Note`, crimson note **plus** the measured number in `plot_method_bars`, `*` suffix in `plot_heatmap` and on `plot_phase_bars` ticks, a crimson ring (label `'non-finite parameters'`) instead of a failure wall in `plot_sweep`, new `non-finite` count column in `scaling_table`, `status` added to `plot_steadiness`'s returned frame. `scaling_table`'s `largest ok P` → **`largest timed P`** (only consumers are notebook prints).
- **`analysis/repair_legacy.py`** (F2, F4). `val_ew` is now index-aligned with the stored `val` curve: `[nan]*offset + ew`, `len(val_ew) == len(losses['val'])`, asserted in `repair_run`; docstring says "plot on the same epoch axis, not shifted". The eq-weight gate now samples only rows with a finite `eq_check` and requires **every batch size** present to contribute one (the reshape depends on `(n_val, batch_size)`), raising `... the repair is unverified` otherwise; `check=0` skips; count in `attrs['n_checked']` and in the log line. I did **not** adopt the suggested `len(cand) >= min(check, len(ok))` — it raises on a legitimate scan that merely diverged a lot, while per-geometry coverage is what protects a new batch geometry.
- **`analysis/paired.py`** (F3). New `_config_runs(df, row)` returns `df.iloc[:0]` when `run_ids` is not list-like (`config_table`'s outer join leaves NaN for all-diverged configs), and `a`/`b` are coerced to float so an empty pairing does not hit `np.isfinite` on an object column. `n = 0`, NaN mean/std, no exception.
- **`tests/test_analysis_offline.py`**: 6 new tests (`_profile_record` gained `method/study/n_params/peak_bytes/params/raw_extra`, `_scan_with_val_batch` gained `seed`).
- **Regenerated all 8 `analysis/legacy_repair/*.parquet`** (aligned `val_ew`); `corrections.csv` numbers unchanged (0.064/0.097/0.416/0.278/0.170/0.105/0.185/0.195 % median), `eq_check_max = 0.0`, `n_checked` = 5·6 scans, 30 (batch-size sweep), 20 (overparam).

### Acceptance tests / observed summaries
`33 passed, 5 skipped, 1 warning in 5.86s` (local) and, via `campaign/run_cpu_tests.sh` with `SV3_CHECK_REAL_SCANS=1`, **`38 passed, 1 warning in 4.08s`** — includes SOAP `step_ms == 6.14` / `step_ms_steady == 5.56` (abs 0.005), `gram_hooks 10.40` (0.05), real CIFAR repair 0.17/0.71/1.1 %, trajectory counts 12.0/11.3/22.4/16.0. New asserts: non-finite Adam/SGD reference still yields `rel_time == rel_mem == 4.0` and heatmap text `1.0*`; sweep line keeps 2 timed points with 1 ring and only the OOM as a wall; `scaling_table` → `largest timed P == 100`, `first failure == 'oom @ P=1,000'`, `non-finite == 1`; `len(val_ew) == len(stored val)` with/without `pre_training_eval`; all-NaN scans raise `unverified` (incl. one batch size all-diverged); paired vs an all-diverged config gives `n=0`, `significant == [True, False]`.

### Deviations from CONTRACTS.md
None new (the implementer's three stand: `uv pip install` fallback instead of `uv sync --inexact --active`; docstrings edited outside `summarize()`; `cache_dir` pointed away from the results root). Two CPU jobs submitted, both through `campaign/run_cpu_tests.sh`.

### Integrator
- New/renamed API: `profile_helpers.TIMED`; `scaling_table` columns `largest timed P`, `non-finite`; `plot_steadiness` returns `status`; `repair_legacy` `attrs['n_checked']`, `val_ew` aligned (index i ↔ `val[i]`, leading NaN when `pre_training_eval`); `paired._config_runs`.
- **Not mine, must change**: `bench/probe_campaign/probe_run.py:230` (`return 0 if rec["status"] == "ok" else 1`) and `bench/probe_campaign/analyse.py:73` (`status == "ok"` filter) must treat `nonfinite` as timed, or the probe campaign discards runs whose `wall_ms` the NPROC/sharding decision depends on. Reuse `profile_helpers.TIMED`/`STATUS_NOTE`.
- Still open from the impl report: `analysis/profile_overview.ipynb`'s two "steady-state mean" markdown cells are false, and notebook cells for `budget`/`paired`/legacy repair are unwritten (I executed no notebooks).