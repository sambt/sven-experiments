**TRACK grid — review fixes applied; all 8 findings dispositioned.**

## Files changed
| file | change |
|---|---|
| `/n/home11/sambt/iaifi/sv3/experiments/experiment_code/grid.py` | `_quiet(verbose)` wraps the copied `process_hparam_config` in `redirect_stdout` (F7: `verbose=False` is now truly silent, copy stays byte-identical so the drift guard still works); `RunSpec.__hash__ = None` + docstring "not hashable; key on run_id" (F6) |
| `/n/home11/sambt/iaifi/sv3/experiments/experiment_code/generic_scan.py` | F8: `else: # hig` → `elif spec.family == "hig":` + final `else: raise AssertionError(f"execute: unhandled family ...")`; except-label now `hp.get('optim_name', spec.family)` so that raise cannot become a KeyError |
| `/n/home11/sambt/iaifi/sv3/tests/test_grid.py` | +10 tests (101→111); drift guard, shard test and docstring rewritten |
| `/n/home11/sambt/iaifi/sv3/tests/golden/legacy_grid_equiv.py` | **new** — the order-oracle producer, committed (F1). SHA pinned to `58fc8e4` (not `HEAD`), `--freeze` required to rewrite goldens, documents the one fragility (it imports against the live `experiment_utils.py`) |
| `/n/home11/sambt/iaifi/sv3/tests/golden/scan_smoke.sh` | **new** — the end-to-end driver (F2), parameterised workdir, refuses to run inside the repo (verified: `refusing to run inside the repo: .../tests/golden/nope`) |
| `tests/golden/{toy_1d_scan,rebuttal_batchsize_polynomial_scan}.order.txt` | headers now name the committed producer + SHA; the 780 / 2640 ids are byte-identical (sha256-checked) |

## Acceptance evidence — `111 passed in 4.60s` (CPU node) / `111 passed in 4.53s` (local); single summary line for the file
- **F5** `test_shard_matches_the_legacy_modulo_counter[2 scans × n=1,2,3,7]`: was a tautology; now `[s.run_id for s in shard(expand_grid(cfg),n,i)] == frozen_order[i::n]` against the two `.order.txt` oracles.
- **F3** `test_copied_helpers_match_the_original` / `..._default_weight_decay_...`: resolve each helper from *either* `experiment_utils.py` or `optim_factory.py`, assert the copy matches **every** definition found, fail readably if found in neither. Verified all 5 futures (both/only-utils/only-factory/neither/drift) behave: neither → "defined in none of [...]", factory drift → "drifted from f.py". New `test_wd_optimizers_filter_tracks_the_default_weight_decay_registry` pins `_WD_OPTIMIZERS == ("AdamW","Muon","MuonW") == set(_DEFAULT_WEIGHT_DECAY)|{"Muon"}`.
- **F2** `test_execute_dispatches_every_family_to_its_own_loop`, `test_execute_keeps_the_per_family_asymmetries`, `test_scan_is_expand_shard_dedup_execute_with_a_per_seed_init_state` (AST, torch-free): dispatch covers `grid.FAMILIES` + raises on a new one; loop per family; `_TO_DEVICE_FAMILIES == FAMILIES-{svd,hig}`; `track_param_norm` present for svd/standard/jd/hig, absent for lbfgs/polyak; `drop_last` svd-only; svd-only `svd_info`/`compiler.reset()`, non-svd-only `empty_cache`; `seed_state` guarded by `self._seed != model_seed`; no `product(` left in `scan`. **Mutation-tested: 7/7 mutants caught** (drop_last on standard, track_param_norm on lbfgs, hig in `_TO_DEVICE_FAMILIES`, bare-else fallthrough, empty_cache on svd, seed_state rebuilt per run, inline product).
- **F6/F7** `test_runspec_is_compared_and_keyed_by_run_id_not_hashed`, `test_expand_grid_is_silent_when_not_verbose`.
- **Re-verified end to end** (`campaign/run_cpu_tests.sh`, log `slurm_logs/devtests/devtest-Jh2QbC.out`): `generic_scan` imports (`execute`/`scan` present); committed `legacy_grid_equiv.py` → `ALL EQUIVALENT`, 11 config/mode cases, no `BAD shard` for the 6 (n,i) settings; smoke → 6 families wrote jsonl+npz, pass 2 printed 6 `[skip]`, `+n_shards=2 +shard_id=1` ran exactly Adam/PolyakSGD/HIG; real root still 734 jsonl.

## Deviations / rejections
- **F1/F2 location**: generator and smoke driver went to `tests/golden/` (owned) instead of `tools/` and `tests/test_scan_smoke.py` (not owned; a torch-importing test file also breaks the "<60 s per file" rule). Integrator may `git mv` them to `tools/`; test docstring references would need updating.
- **F4 rejected as out-of-track**, unchanged: `experiments/experiment_code/__init__.py` (`from .generic_scan import scan`) makes `import ...grid` pull torch. **Integrator: drop/lazify that re-export**; until then `tools/reconcile.py` must path-load `grid.py` (see `tests/test_grid.py::_load_grid`).
- Other integrator notes from `grid.impl.md` stand unchanged; new names added: `grid._quiet`, `generic_scan` unchanged API. Before editing `rebuttal_batchsize_polynomial_scan.yaml`/`toy_1d_scan.yaml` read the **RE-FREEZING** block at the top of `tests/test_grid.py` (4 numbered items + the `--freeze` command).