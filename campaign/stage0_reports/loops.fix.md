**Track `loops` — fixer pass. All findings resolved; `45 passed` on a CPU node (was 33).**

**Changed:** `/n/home/anon/sven-experiments/experiments/experiment_code/experiment_utils.py` (loops region only; the `_CUSTOM_OPTIMIZERS`…`build_standard_optimizer` region starts at line 995, last diff hunk ends at 991 — untouched), `/n/home/anon/sven-experiments/tests/test_loops_contract.py` (16→27 tests), `/n/home/anon/sven-experiments/tests/test_evaluate.py` (8→9). Unchanged: `experiments/nn/norm_utils.py`, `experiments/optimizers/hig.py`, `tests/test_bn_policy_loops.py`.

**Summary lines observed:** `tests/test_loops_contract.py` → `27 passed in 134.58s` (local); `tests/test_evaluate.py` → `9 passed in 18.14s`; `tests/test_bn_policy_loops.py` → `9 passed in 14.03s`; combined via `campaign/run_cpu_tests.sh` → `45 passed in 15.30s`, `exit=0`, log `/n/home/anon/sven-experiments/slurm_logs/devtests/devtest-8wMpRV.out`.

**Fixes (finding → change → test that pins it)**
1. *Check after the update* (medium): new `_pre_step_check(...)` runs the example-weighted stats and raises **before** the update in the non-closure branch of `train_loop_standard` and in `svd`/`hig`/`jd`; the stats are threaded into `_record_train_batch(..., stats=...)` so `.item()` still happens once per batch. Pinned by `test_the_update_never_sees_a_nonfinite_loss[standard,jd]` (`opt.updates == err.step`, i.e. no step on the diverged batch) and `test_wrapper_loops_classify_divergence_before_the_linalg[svd,hig]` (stub optimizer that raises `RuntimeError("linalg…")` if handed a non-finite loss; `DivergedError` wins). **Rejected sub-claim:** the LBFGS "off-by-one". The contract defines `DivergedError(step)` by the non-finite *batch loss*, and for a closure optimizer the pre-update loss at the step whose line search blew up is still finite, so the first non-finite batch loss genuinely is the next step. Documented and pinned by `test_closure_divergence_is_reported_at_the_first_nonfinite_batch_loss`.
2. *`evaluate()` trusts the wrapper* (medium): new `_forward_module(forward_fn)` resolves `nn.Module` → bound method's `__self__` → `__self__.model`. `test_evaluate_guards_a_wrapper_that_forgets_eval_mode` (deliberately sloppy wrapper: buffers bit-identical, value equals the eval-mode one, mode restored).
3. *`_apply_log_schedule` silent no-op* (medium): `hasattr` guard dropped, set unconditionally. `test_log_schedule_is_set_even_on_an_optimizer_that_never_had_the_flag`, `test_log_schedule_failure_is_loud_not_silent` (read-only property → `AttributeError`).
4. *Syncs untested* (medium): `test_batch_timers_synchronise_the_device_twice_per_batch[standard,svd]` (monkeypatched `_cuda_active`+`torch.cuda.synchronize` counter: exactly 8 for 2 epochs × 2 batches, 0 when inactive) and `test_cuda_active_needs_cuda_to_be_both_available_and_the_device`. `_apply_log_schedule` moved outside the svd timed region (now identical in all four loops).
5. *Partial record on failure* (medium, loop half): `_partial_record_on_divergence` runs `_finish_losses` on `DivergedError` and attaches the dict as `err.losses`, so a diverged run has `total_time/avg_*/val_final/val_best` and survives even an old-style call. `test_absurd_lr_…` (extended), `test_diverged_curves_survive_without_a_caller_owned_dict`.
6. Low: `_compute_acc` deleted (rationale folded into `_correct_count`); mid-epoch eval time now recorded as `eval_step_times` (pinned in the C-E4 test, absent otherwise); `val_best_epoch` → **`val_best_index`** and `val_last3_mean` is `None` for `len(val) < 4`.

**Deviations from CONTRACTS.md** (unchanged from the impl report except): curve keys add `eval_step_times`; `val_best_epoch` no longer exists; `val_last3_mean` may be `None`. The standard loop now pays one extra device sync per batch (the pre-update `.item()`); it is inside the timed region, so `avg_train_time` includes it uniformly for every first-order baseline.

**Integrator / other tracks**
- Runner (`generic_scan.py`): bind `losses={}` before the `try` and pass `losses=losses`; **classify `DivergedError` first** (it subclasses `RuntimeError`; message is "non-finite training loss at optimizer step N"); fall back to `err.losses`. Do not launch these loops with `stop_on_nonfinite=True` before C-R1 writes diverged records, or pass `stop_on_nonfinite=False` meanwhile. Drop dead `'val_batch'/'batch_times_val'` from `_DIAG_LOSS_KEYS` (line 151).
- Analysis: `avg_batch_time_val` is gone; add `train_times/avg_train_time/eval_times/avg_eval_time` to `OUTCOME_COLUMNS` (`style.py:453`) and `STANDALONE_QUANTITIES` (`scan_analysis.py:928`) and compare method speed on **`avg_train_time`** — `avg_epoch_time` now contains val+test+train_eval evaluation (`epoch_times[i] - eval_times[i]` is train+loader). `analysis/repair_legacy.py`'s `val_batch` reads are legacy-only and must stay.
- Still integration-only: BatchNorm + Sven through `train_loop_svd` (unit-tested on an MLP and on stubs only).

Meta: review-agent concurrency is an orchestrator setting, not a track constraint — these three test files are independent and run in one pytest invocation.