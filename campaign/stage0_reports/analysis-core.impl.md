## TRACK analysis-core — done

**Files** (only my track's): `/n/home/anon/sven-experiments/analysis/{style.py,analysis_helpers.py,scan_analysis.py,sv_diagnostics.py}` (+953/−208), new `/n/home/anon/sven-experiments/tests/test_analysis_core.py` (746 lines).

**Acceptance tests** — `.venv/bin/python -m pytest tests/test_analysis_core.py -q` → `19 passed, 2 skipped in 3.92s`; via `campaign/run_cpu_tests.sh env SV3_CHECK_REAL_SCANS=1 …` → **`21 passed in 4.94s`**.

| test | asserts |
|---|---|
| `allow_list_grouping_ignores_provenance` | partition (sets of run_ids) identical with/without 20 schema-2 provenance columns; none of `status/run_hash/git_sha/host/…/n_test/steps_per_epoch` in `config_columns`; `config_table` same 4 rows, same n_seeds, same means |
| `average_over_seeds_ignores_provenance` | same for `style.average_over_seeds` |
| `unlisted_varying_column_is_reported` | a new varying knob prints a warning naming `HPARAM_COLUMNS` (allow-list's only failure mode made visible) |
| `selector_assertion_fires_for_test_metrics` | `ValueError` from `config_table`, `Scan.configs`, `best_sven`, `best_per_method`, `summary_table`, `plot_time_vs_k(quantity=)`, `knob_table` for `final_test_loss/acc/test/test_acc`; test outcomes still appear as columns |
| `status_diverged_counts_and_excludes` | finite-curve `status:diverged` → diverged only via status, n_diverged=1, out of the mean |
| `status_oom_error_excluded_and_counted` | oom/error → `n_failed=2`, `n_diverged=0`, their 1e-9 partial curves not in means, config ineligible, seed band not truncated |
| `manifest_makes_missing_runs_visible` | `n_missing`/`attempted` from manifest union; a config with **no** file listed in `.attrs['missing_configs']`; `Scan.missing_run_ids`; empty selection → empty table |
| `legacy_records_still_load` | no status/manifest/test/n_train → old behaviour exactly (curve divergence, `n_missing=seeds−records`, `epoch_times`, documented tie-break) |
| `partial_curve_robustness` | `Scan.n_epochs`=4 with a 1-epoch first row; `epoch_axis` 0..4 (val) / 1..4 (train) incl. truncated curves; `sv.n_epochs({})==0` |
| `results_root_from_env`, `read_only_root_still_loads`, `cache_dir_redirects_the_cache` | env resolution; `chmod 0o555` root loads + warns; cache lands in `cache_dir` |
| `sv_min_kept_is_separate_from_sv_min` | both keys survive `load_diagnostics` **and** `slim=False` with distinct values (F20) |
| `full_width_spectrum_plot_…` / `legacy_truncated_…_unchanged` | full width: x max=1.0 (÷width), k line at (k−1)/(width−1), rtol=1e-4 and 1e-7 floor lines; legacy: x max=2/7 (÷k), warning fires, no k/floor lines |
| `utr_plot` | `(n_epoch,width)`, descending, k cut, `(None,None)` on legacy |
| `step_and_example_axes` + fallback | 25/800 per epoch → steps `[25..100]`, examples `[800..3200]`, val offset 0; `train_times` becomes the auto time key; **never guessed** (None) when neither recorded |
| `notebook_surface_smoke` | all 14 scan-notebook plot helpers + 8 study helpers on a schema-2 scan (manifest, diverged, oom, test, train_times, standalone join) |
| `real_scan_grouping_unchanged[toy_1d_scan,mnist_scan_ce]` | 734 runs/148 groups sizes {1,4,5}; 1040/208 sizes {5} — allow-list == legacy auto-detect == run_id partition |

**Extra evidence:** swept **all 33** dirs of `experiment_results/` cold on a CPU node (cache redirected outside it): `CHANGED SCANS: []`. `config_key` is coarser than column-grouping on 5 legacy dirs (critbatch×2, nanogpt_speedrun, mnist_microbatch_ce, mnist_paramfrac_ce) — two record generations per config, F16 schema drift; harmless (no manifests there).

**Deviations from CONTRACTS.md:** none substantive. (a) 6-line manifest reader duplicated in `style` instead of importing `claims.read_manifest_union` — `experiments.experiment_code.__init__` imports torch. (b) `eligible` is now per-config `attempted` instead of the scan-wide seed count — provably identical on legacy data, correct with manifests. (c) No C-A5 time-to-target (task: axes only); notebooks/`profile_helpers.py` untouched.

**Integrator notes.** New API: `style.resolve_results_root(root=None)`, `RESULTS_ROOT_ENV='SV3_RESULTS_ROOT'`, `load_results(..., cache_dir=None)`, `manifest_run_ids(name, results_root=None)`, `config_key(run_id)`, `expected_per_config(run_ids)`, `missing_run_ids(df, expected)`, `status_of(row)`, `is_failed(status)`, `is_diverged(train, val, status=None)`, `hparam_columns(df, seed_col, extra_skip=(), warn=True)`, `HPARAM_COLUMNS/PROVENANCE_COLUMNS/BACKEND_COLUMNS/OUTCOME_COLUMNS`, `assert_selection_metric(metric, where)`, `SV_MIN_KEYS`, `FLOAT32_NOISE_FLOOR`; `scan_analysis.usable(rows)`, `steps_per_epoch/examples_per_epoch(rows)`, `axis_label`, `has_curve`, `plot_sv_utr`, `Scan.{expected_run_ids,has_test,missing_run_ids}`, `load_scan(..., cache_dir=)`; `analysis_helpers.{expected_run_ids,missing_configs}`, `config_table(..., expected=None, results_root=None)` → new `n_failed/finished/attempted` columns; `sv_diagnostics.{epoch_utr,plot_epoch_utr}`. **Required of other tracks:** (1) `{scan}/manifest/{job}.json` with `run_ids` (claims.write_manifest already matches) or `attempted` falls back to the seed count; (2) any NEW hyperparameter column must be added to `style.HPARAM_COLUMNS` (a varying unlisted column only warns) — `bn_mode`, `split_seed`, `data_seed`, `num_epochs`, `n_data`, `n_train` are already listed, `n_val/n_test/n_params/steps_per_epoch/actual_param_fraction` are provenance; (3) `_split_diagnostics` must write `utr` NaN-padded 2-D **row-aligned with `svs_step`** and `sv_min_kept` under exactly those npz names, never renaming `sv_min`; (4) **`bench/select_best_configs.py` (not mine)** averages `final_val_loss` over all rows of the best config — switch to `scan_analysis.usable(r)` so oom/error rows cannot enter, and export `SV3_RESULTS_ROOT` when selecting against the fresh root; (5) `analysis/profile_helpers.py` keeps its own hard-coded `'../profile_results_v2'` (C-T2 track's call whether to make it env-driven).