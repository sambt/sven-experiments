export const meta = {
  name: 'sven-paper-update',
  description: 'ICLR manuscript update: plan -> parallel assets (figures/tables/number macros) and appendix drafts -> integrate into a new blue-marked tex -> compile -> 3-lens adversarial review -> fix',
  phases: [ { title: 'Plan', model: 'opus' }, { title: 'Assets and drafts', model: 'opus' }, { title: 'Integrate', model: 'opus' }, { title: 'Review', model: 'opus' }, { title: 'Fix', model: 'opus' } ],
}
const HEAD = `Repo /n/home/anon/sven-experiments (branch robustness-campaign); the manuscript lives in ./iclr_manuscript (its OWN git repo, Overleaf-synced: never commit/push/pull there). READ FIRST, fully: campaign/PAPER_CONTRACTS.md (binding: what the user asked, blue markup, numbers-as-macros, claims discipline, LaTeX build). Context you will need: iclr_manuscript/iclr2026_conference.tex (the current manuscript - read it completely, including appendices, to absorb its style and notation), rev1.md rev2.md rev3.md (the NeurIPS reviews this revision answers) and REBUTTALS.md section 5-6 (promised clarifications), campaign/ANALYSIS_PLAN.md + campaign/ANALYSIS_CONTRACTS.md, campaign/analysis_reports/B.fix.*.md and A.fix.*.md (results, caveats, corrected numbers), analysis/WHAT_CHANGED.md, EXPERIMENTS.md (protocol and scans as actually run), analysis/tables/ (exported tables), the analysis notebooks and helper modules in analysis/. A parallel workflow is still refreshing three inputs (CIFAR-CE Sven re-selection, a Fig-5 rerun at the selected configuration, and an optimizer re-profile into profile_results_v3/): anything touching those must be produced by re-runnable code so a later refresh updates it.
`
const TAIL = `
FINAL REPORT (return value, dense markdown <= 600 words): files created/changed, what you verified by running, open issues, and notes for the integrator.`

phase('Plan')
const plan = await agent(`${HEAD}
TASK "paper-plan". Write campaign/PAPER_PLAN.md: the concrete blueprint every other agent will follow. It must contain:
1. CLAIMS MAP: every empirical claim the revised paper will make (abstract, intro, experiments, conclusion, appendices), each with its evidence (table/figure + analysis function + the number today) and the caveat it must carry. Include the claims the CURRENT manuscript makes that the fresh data no longer support, and the replacement wording direction. Decide the headline framing (the user suggests polynomial + MNIST + nanoGPT as the main results figure, with 1D regression moved to the appendix) and justify it in two sentences.
2. SECTION-BY-SECTION EDIT PLAN for the new file (abstract, intro incl. the paper-organisation paragraph, methodology incl. the dimensionality statement R3 asked for [B = batch, P = parameters, M in R^{BxP}, one row per sample of the scalar residual; multi-output models], the corrected cost statement [classic randomized path O(kN|D|) vs Gram backend: capture + BxB eigh, independent of k] and the main-text mention of the Gram implementation, related work additions if any [SOAP/Shampoo/Muon are now baselines], experiments [datasets incl. the redefined polynomial with variance-normalised monomials, nanoGPT; train/val/test protocol, selection on validation, confirmation seeds; baselines list incl. AdamW, SGD+momentum, Muon/MuonW with the grouping rule, SOAP, Shampoo, K-FAC, stochastic L-BFGS, Polyak, JD, HIG], results, the reworked singular-value-spectrum discussion [full-width spectra, residual energy captured by the top-k directions, evolution over training, probe-set spectra along Sven vs baseline trajectories, rank actually used vs k and rtol], conclusion/limitations), and for each APPENDIX (existing ones updated: experiment details, additional results/all-seed plots, MNIST-CE, CIFAR-10, kappa, profiling, memory mitigation; NEW ones: Gram-trick implementation, dataset-level overparameterisation P > N [R1's crux], batch-size sensitivity [R2], tuning budget and paired comparisons, robustness/divergence, transformers: nanoGPT details and GPT-2-small, Fig-5 parameter-fraction at scale with memory/time [R1 asked for main text: decide main vs appendix given the 9-page limit and say why], spectra deep-dive, reproducibility notes). For each: what changes, which assets it uses, approximate length.
3. ASSET INVENTORY: a table of every figure, table and number-macro group to generate: id, output path under iclr_manuscript/ (figures_iclr/<group>/<name>.pdf, tables_v2/<name>.tex, numbers_v2_<module>.tex), the analysis function(s) to call, panel layout/size, which asset module owns it (main | reviewer | large | spectra), main text vs appendix, and the caption's key message. Keep the main text within 9 pages: say which existing main-text figure is replaced by which new one.
4. A page budget and a list of risks.
Be decisive; do not leave choices open. Do not write LaTeX prose yet.
${TAIL}`, { label: 'plan', phase: 'Plan', model: 'opus' })

phase('Assets and drafts')
const ASSET_COMMON = `RESUME NOTE (2026-09-21): an earlier attempt at this task was interrupted by a node failure - PARTIAL WORK MAY ALREADY EXIST in your owned paths (analysis/paper_assets/, tests/test_paper_assets_*.py, iclr_manuscript/figures_iclr|tables_v2|numbers_v2*.tex): inspect it first, keep what is right, finish the rest. The session now runs on a LOGIN node: run NOTHING heavy locally (no torch imports, no notebook execution, no full asset builds, no pdflatex loops longer than a minute) - use campaign/run_cpu_tests.sh <cmd...> for all of that (it submits a short CPU job and prints the log). The three refreshed inputs are now FINAL on disk: CIFAR-CE Sven re-selected (bench/best_configs.json: k=128, lr=0.5, rtol=0.3, with its own confirm/timing/diag runs), the Fig-5 rerun at the selected configuration (15 new k=128 runs beside the 15 old k=64 ones), and profile_results_v3/ (complete). The blueprint is campaign/PAPER_PLAN.md (read it fully; its ASSET INVENTORY rows for your module are your work list - implement all of them). Assets are produced by the package analysis/paper_assets/ (create the package dir if it does not exist; shared conventions live in analysis/paper_assets/common.py which the "main" module owner writes FIRST THING - if you are not that owner and it does not exist yet, wait a few minutes and re-check, then import from it: output dirs, savefig in the manuscript's visual language, macro writer write_macros(path, dict) that emits \\newcommand lines with a provenance comment, table writer with booktabs). Your module exposes build(root) and is runnable as "python -m paper_assets.<module>" from analysis/; it must be re-runnable and must never hand-type a number. Every number in a table or macro must be traceable: emit next to each generated file a small <name>.provenance.json (function called, scan dirs read, selection file hash, timestamp). Tests in tests/test_paper_assets_<module>.py (synthetic fixtures where cheap; at minimum: macro names are valid LaTeX identifiers, no NaN/None leaks into macros or tables, tables compile-safe [escaped underscores, no stray %]). Run your module for real and LOOK at every figure you produce (open the PNG you also save next to each PDF under agent_lab/, not in the manuscript dir): legible at 0.32\\linewidth, legends not covering data, log axes sensible, method names via style.method_label, Sven black. Do not edit the tex file.`
const drafts = await parallel([
  () => agent(`${HEAD}
TASK "assets-main" (module analysis/paper_assets/main.py + common.py + __main__.py that runs all modules and writes iclr_manuscript/numbers_v2.tex which \\inputs the per-module macro files). ${ASSET_COMMON}
Scope: the headline results figure(s) (validation loss vs epoch and vs standalone wall time for the headline tasks with the new baselines; confirmation seeds; decide per the plan which methods are drawn in the main-text panels and which only in the appendix versions with all methods), the all-methods/all-seeds appendix versions for every headline scan incl. 1D regression and MNIST-CE, the k-sweep and rtol-sweep panels (Sven hyperparameter landscape incl. the extension-round points), the headline results TABLE(S) (per scan: method, selected config, val loss, test loss, test accuracy where defined, finished/attempted, s/epoch, peak memory; confirmation seeds with tuning seeds beside), the ranking summary table, and the macro group for every number the abstract/intro/results/conclusion will quote.
${TAIL}`, { label: 'assets:main', phase: 'Assets and drafts', model: 'opus' }),
  () => agent(`${HEAD}
TASK "assets-reviewer" (module analysis/paper_assets/reviewer.py). ${ASSET_COMMON}
Scope: dataset-level overparameterisation P > N (loss, test loss, train loss and time-to-target vs P/N; Sven's rank vs P/N; significance of rank-1 vs rank-2 gaps; divergence rates), batch-size sensitivity, kappa at matched effective step, micro-batch and parameter-fraction MLP studies, tuning-budget disclosure (distinct trajectories, best-of-n curves, equal-budget table), paired differences vs Sven, time-to-target tables, robustness/divergence accounting per method and where Sven's divergences sit (rtol/lr). Use analysis/reviewer_figs.py, headline.py, headline_figs.py, budget.py, paired.py.
${TAIL}`, { label: 'assets:reviewer', phase: 'Assets and drafts', model: 'opus' }),
  () => agent(`${HEAD}
TASK "assets-large" (module analysis/paper_assets/large.py). ${ASSET_COMMON}
Scope: CIFAR-10/ResNet18 (both losses: curves vs epoch and vs time, val/test accuracy, train-vs-val gap, results table, Sven landscape incl. the rtol extension), the parameter-fraction study at scale (Fig-5: both configurations once the rerun at the selected configuration exists - write the code so it shows whatever configurations are on disk and marks the selected one; accuracy/loss vs ACTUAL parameter fraction with seed bands, peak memory and step time, divergence counts), nanoGPT (curves vs step and vs time, table, lr sensitivity), GPT-2-small (val/test vs step for the best lr per method + the five Sven lrs, final table, wall-clock and memory; single-seed framing). Use analysis/large_figs.py and headline.py. CIFAR-CE is being re-selected right now and Fig-5 is being rerun: your code must pick up the new state when re-run (no cached picks).
${TAIL}`, { label: 'assets:large', phase: 'Assets and drafts', model: 'opus' }),
  () => agent(`${HEAD}
TASK "assets-spectra" (module analysis/paper_assets/spectra.py). ${ASSET_COMMON}
Scope: (1) the singular-value story for the main text and a deep-dive appendix: full-width normalised spectra over training for the headline tasks (from the <scan>_diag runs) with the k cut, the rtol line and the float32 noise floor; the residual-energy fraction captured by the top-k directions over training (the mechanism figure); rank actually used vs k / rtol; MNIST label-regression vs cross-entropy spectra; probe-set spectra along the trajectories of Sven vs Adam vs MuonW vs HIG from analysis/ckpt_tools.py caches (analysis/ckpt_spectra/), effective rank / sigma_B/sigma_1 over training, distance from initialisation and parameter norm (report the null result honestly). Use analysis/spectra_figs.py, sv_diagnostics.py, ckpt_tools.py. (2) PROFILING: the memory and step-time study from the optimizer profile - methods overview per architecture (toy/polynomial/MNIST/nanoGPT/CIFAR), Sven backends (Gram hooks vs full-Jacobian Gram vs chunked vs classic randomized SVD), scaling with parameter count (MNIST width, nanoGPT width), batch size, micro-batch, parameter fraction, k. Use analysis/profile_helpers.py with its results-root resolution: v3 (profile_results_v3/) is being produced right now - your code must use v3 when profile_status() says it is complete and v2 otherwise, and must state in a macro which one was used; include a v2-vs-v3 note only in provenance, not in the paper. Replace the manuscript's old profiling figures (figures_v2/*_profiling) with equivalents from the new profile.
${TAIL}`, { label: 'assets:spectra', phase: 'Assets and drafts', model: 'opus' }),
  () => agent(`${HEAD}
TASK "draft-gram-appendix". Write iclr_manuscript/sections_v2/app_gram.tex (a fragment to be \\input or pasted by the integrator; all of it is new text, so wrap it in the newtext environment per the contracts) = the full appendix on the memory-optimised Gram implementation of Sven, plus iclr_manuscript/sections_v2/main_gram_paragraph.tex = the short main-text mention (one paragraph, to go at the end of the methodology section) and a corrected cost statement. Derive EVERYTHING from the code, not from memory: sven/sven/opt/sven.py (SvenGram, SvenGramReg, Sven classic), sven/sven/nn/gram_wrapper.py (capture modes hooks / chunked / full; _hooks_kernel; per-layer kernels for Linear / Conv2d / norm layers; delta_from_w = J^T w via one backward pass; micro-batch pooling of the kernel; element / row / tensor parameter masks under the Gram formulation; BatchNorm handling), sven/sven/nn/sven_wrapper.py, sven/tests/test_torch_gram*.py (exactness checks and their tolerances), campaign/stage0_reports/sven-nn.*.md and sven-opt.*.md. Content: (a) the identity: with M in R^{BxP} the per-sample residual Jacobian, the truncated pseudoinverse update M^+ r = M^T U_k S_k^{-2} U_k^T r needs only the BxB Gram G = M M^T (eigendecomposition in float64) and one vector-Jacobian product M^T w - so the BxP Jacobian is never materialised; (b) how G is assembled without per-sample gradients for layers whose per-sample gradient is an outer product (dense and convolutional layers: G gets the Hadamard product of activation and output-gradient Gram matrices, summed over layers; biases; normalisation affine parameters), its cost (one forward, one backward to layer outputs with B-fold... state precisely what the code does) and memory (activations + O(B^2) per layer instead of O(BP)); (c) the architectural restriction (per-sample-decoupled layers; BatchNorm in batch-statistics mode couples samples -> the chunked/full capture fallback, which computes G from Jacobian blocks of bounded size) ; (d) truncation by k and rtol in the Gram eigenbasis, numerical floor (float32 capture, float64 Gram: singular values below ~1e-7*sigma_max are round-off) ; (e) micro-batching and parameter masks in this formulation; (f) equivalence to the explicit-Jacobian update (tests: agreement to ~1e-6 relative in float32, 1e-13 in float64) ; (g) an algorithm box in the manuscript's algpseudocode style; (h) measured cost with macros (step time and peak memory vs Adam/SGD for the MLP, nanoGPT and scaling with parameter count; placeholders named per campaign/PAPER_PLAN.md's macro scheme for the spectra/profiling module - list the macro names you need in your report). Match the manuscript's notation exactly (read its methodology section: M^alpha_i, |D|, N, eta, rtol, k) - note the manuscript uses N for parameter count and |D| for batch/dataset size; keep ITS symbols. Concise, precise, no marketing.
${TAIL}`, { label: 'draft:gram-appendix', phase: 'Assets and drafts', model: 'opus' }),
  () => agent(`${HEAD}
TASK "draft-exp-details". Write iclr_manuscript/sections_v2/app_exp_details.tex (fragment; replaces the body of the existing "Experiment Details" appendix: keep any sentence that is still true in black by copying it verbatim OUTSIDE the newtext environment, put changed/new text inside it) and iclr_manuscript/sections_v2/app_reproducibility.tex (new short appendix). Derive every fact from the repo: EXPERIMENTS.md (rewritten from configs and records), experiments/configs/*.yaml, campaign/grid_counts.md, campaign/CONTRACTS.md (decisions), experiments/datasets/all_datasets.py (exact dataset definitions: 1D function; polynomial = all 210 monomials of total degree <= 4 in 6 variables with coefficients N(0,1)/sqrt(E[m^2]) i.e. variance-normalised monomials, x ~ N(0,1), targets standardised by a fixed pool; MNIST 50k/10k/10k; CIFAR-10 45k/5k/10k; Shakespeare character-level 80/10/10; FineWeb-edu GPT-2 tokens with disjoint splits), experiments/nn/ (architectures and parameter counts), experiments/experiment_code/optim_factory.py (baseline definitions: AdamW wd 0.01, MuonW wd 0.1, Muon grouping rule and lr adjustment, SGD+momentum 0.9, stochastic minibatch L-BFGS with strong-Wolfe line search, Polyak, JD/UPGrad, HIG, SOAP/Shampoo/K-FAC settings). Content: datasets and splits; architectures; the evaluation protocol (selection by seed-mean final validation loss; a run counts as failed if it ends non-finite or above 10x its initial loss; configurations need more than half their seeds; test metrics never used for selection; selected configurations re-run on 5 fresh seeds [and 3 fresh data draws for the synthetic tasks] - these are the reported numbers; identical initialisation and data order across optimizers per seed; example-weighted metrics; BatchNorm policy), full hyperparameter grids per optimizer and task as TABLES (generated by hand from the configs is acceptable here because they are configuration facts, but double-check each against expand_grid via experiments/experiment_code/grid.py), number of runs, compute (GPU types, total GPU-hours from the records - compute it), wall-time measurement protocol (standalone runs, logging off), reproducibility notes (hash-keyed results, failure records, cross-GPU non-determinism for bf16/line-search/decomposition-based methods), code availability unchanged. Match the manuscript's style; concise.
${TAIL}`, { label: 'draft:exp-details', phase: 'Assets and drafts', model: 'opus' }),
])

phase('Integrate')
const integrate = await agent(`${HEAD}
RESUME NOTE (2026-09-21): an earlier attempt at this task was interrupted by a node failure - PARTIAL WORK MAY ALREADY EXIST in the manuscript directory: inspect it first, keep what is right, finish the rest. The session now runs on a LOGIN node: run NOTHING heavy locally (no torch imports, no notebook execution, no full asset builds, no pdflatex loops longer than a minute) - use campaign/run_cpu_tests.sh <cmd...> for all of that (it submits a short CPU job and prints the log). The three refreshed inputs are now FINAL on disk: CIFAR-CE Sven re-selected (bench/best_configs.json: k=128, lr=0.5, rtol=0.3, with its own confirm/timing/diag runs), the Fig-5 rerun at the selected configuration (15 new k=128 runs beside the 15 old k=64 ones), and profile_results_v3/ (complete). TASK "integrate" - you are the single author of iclr_manuscript/iclr2026_conference_v2.tex. Inputs: campaign/PAPER_PLAN.md (the blueprint - follow it), the asset modules' outputs (iclr_manuscript/figures_iclr/, tables_v2/, numbers_v2*.tex; regenerate with "cd analysis && ../.venv/bin/python -m paper_assets" if anything is missing), the drafted fragments in iclr_manuscript/sections_v2/, and these reports from the asset/draft agents:
${JSON.stringify(drafts.map(d => (d || '').slice(0, 2500)))}
Do: (1) copy iclr2026_conference.tex to iclr2026_conference_v2.tex; add to the preamble the blue markup (\\new, newtext environment), \\input{numbers_v2}, the second bib file; (2) carry out the section-by-section plan: abstract, introduction (incl. the organisation paragraph), methodology (dimensionality statement, corrected cost statement, Gram paragraph), related work touch-ups, experiments (new headline figure and table, datasets incl. nanoGPT and the redefined polynomial, protocol, baselines, results discussion with the honest comparison, the reworked singular-value discussion with the new figures), conclusion + a limitations paragraph; appendices: update the existing ones (experiment details, additional results, MNIST-CE, CIFAR-10 incl. the parameter-fraction study, kappa, profiling with the new profile, memory mitigation) and add the new ones (Gram implementation, overparameterisation, batch size, tuning budget + paired comparisons + time-to-target, robustness, transformers incl. GPT-2-small, spectra deep-dive, reproducibility) - every new study gets its figure(s)/table and a short, precise text stating what was run and what it shows, with numbers as macros; (3) keep unchanged text byte-identical and black; all edits blue per the contracts; replaced figures keep labels; remove references to figures that no longer exist; (4) compile (pdflatex, bibtex, pdflatex x2) until there are no errors, no undefined references/citations, no overfull boxes wider than 10pt in the main text; main text <= 9 pages (move material to appendices if needed and say what you moved); (5) write iclr_manuscript/CHANGES_v2.md: a section-by-section summary of what changed and why, the list of every claim that was weakened or strengthened with its evidence, and anything the authors must decide (e.g. title, abstract wording, which figure is Fig. 1).
Style: the authors' voice - concise, confident, not apologetic, no hype; do not omit important details; physics-style "we". Do not invent results: if an asset is missing, generate it or leave a clearly marked blue TODO and list it.
${TAIL}`, { label: 'integrate', phase: 'Integrate', model: 'opus' })

phase('Review')
const LENSES = [
  { key: 'claims', text: 'CLAIMS AUDIT. Extract every empirical statement and number in the blue text of iclr2026_conference_v2.tex (abstract to appendices) and in the generated tables/macros; trace each to the raw records (experiment_results/<scan>*/, bench/best_configs.json, profile_results_v*/) with your own short scripts for at least 25 numbers spread over all sections, and check every qualitative claim against campaign/PAPER_CONTRACTS.md "Claims discipline" and the caveats in campaign/analysis_reports/B.fix.*.md. Flag: overclaims, cherry-picked comparisons (tuning vs confirmation seeds, val vs test, per-epoch vs wall time, equal-budget), missing caveats (single seed, remaining grid edges, off-grid points, cross-GPU non-determinism), stale numbers, macros that expand to the wrong quantity, and places where a reviewer from rev1-3.md would object. Also check that the three NeurIPS reviews\' concrete requests are each answered somewhere in the paper (list request -> location).' },
  { key: 'markup-and-text', text: 'MARKUP AND WRITING AUDIT. (a) Diff iclr2026_conference_v2.tex against iclr2026_conference.tex programmatically: every inserted or changed span must be inside \\new{...} or a newtext environment (or be a blue caption); every unchanged sentence must be byte-identical and NOT blue; list violations with line numbers. Removed claims must not leave dangling references. (b) Writing: consistency of notation with the original methodology section (N parameters, |D| data/batch, M, k, rtol, eta, kappa), concision (flag verbose or repetitive passages, marketing tone, apologetic tone), correctness of the Gram-trick derivation and algorithm box against the code in sven/sven/nn/gram_wrapper.py and sven/sven/opt/sven.py, correctness of dataset/protocol/baseline descriptions against experiments/ and EXPERIMENTS.md, anonymity (no names, paths, cluster or repo identifiers beyond the existing anonymised URLs).' },
  { key: 'latex-and-figures', text: 'BUILD AND FIGURE AUDIT. Compile the v2 file from scratch in a scratch copy of iclr_manuscript/ (pdflatex, bibtex, pdflatex x2): report errors, undefined references/citations, duplicate labels, overfull boxes, the main-text page count (limit 9) and total pages. Convert pages to images (pdftoppm if available, else another route) and LOOK at every figure and table: legibility at printed size, axis labels/units, legends, colour consistency (Sven black), log scales, panels that are empty or dominated by one outlier, tables that overflow margins, captions that do not match what is plotted, figure order vs first reference, float placement pile-ups. Check that every file the tex needs is inside iclr_manuscript/ (Overleaf-ready) and that "python -m paper_assets" re-runs cleanly and reproduces the same macro values.' },
]
const reviews = (await parallel(LENSES.map(l => () => agent(`${HEAD}
RESUME NOTE (2026-09-21): an earlier attempt at this task was interrupted by a node failure - PARTIAL WORK MAY ALREADY EXIST in the manuscript directory: inspect it first, keep what is right, finish the rest. The session now runs on a LOGIN node: run NOTHING heavy locally (no torch imports, no notebook execution, no full asset builds, no pdflatex loops longer than a minute) - use campaign/run_cpu_tests.sh <cmd...> for all of that (it submits a short CPU job and prints the log). The three refreshed inputs are now FINAL on disk: CIFAR-CE Sven re-selected (bench/best_configs.json: k=128, lr=0.5, rtol=0.3, with its own confirm/timing/diag runs), the Fig-5 rerun at the selected configuration (15 new k=128 runs beside the 15 old k=64 ones), and profile_results_v3/ (complete). YOU ARE AN ADVERSARIAL REVIEWER of the updated manuscript - READ-ONLY (edit nothing). The integrator's report:
---
${integrate}
---
${l.text}
Severity: high = wrong/unsupported claim, broken build, unmarked edit, or a figure that misleads; medium = missing caveat or content, inconsistency, poor legibility; low = nit (max 5). Be concrete: location (line or figure/table id), what is wrong, how to fix.`, { label: `review:${l.key}`, phase: 'Review', model: 'opus', schema: { type: 'object', properties: { verdict: { type: 'string', enum: ['pass', 'needs_fixes'] }, summary: { type: 'string' }, findings: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['high', 'medium', 'low'] }, location: { type: 'string' }, issue: { type: 'string' }, suggested_fix: { type: 'string' } }, required: ['severity', 'location', 'issue'] } } }, required: ['verdict', 'summary', 'findings'] } })))).filter(Boolean)

phase('Fix')
const serious = reviews.flatMap(r => r.findings || []).filter(f => f.severity !== 'low')
const fix = await agent(`${HEAD}
RESUME NOTE (2026-09-21): an earlier attempt at this task was interrupted by a node failure - PARTIAL WORK MAY ALREADY EXIST in the manuscript directory: inspect it first, keep what is right, finish the rest. The session now runs on a LOGIN node: run NOTHING heavy locally (no torch imports, no notebook execution, no full asset builds, no pdflatex loops longer than a minute) - use campaign/run_cpu_tests.sh <cmd...> for all of that (it submits a short CPU job and prints the log). The three refreshed inputs are now FINAL on disk: CIFAR-CE Sven re-selected (bench/best_configs.json: k=128, lr=0.5, rtol=0.3, with its own confirm/timing/diag runs), the Fig-5 rerun at the selected configuration (15 new k=128 runs beside the 15 old k=64 ones), and profile_results_v3/ (complete). TASK "paper-fixer". You own iclr_manuscript/iclr2026_conference_v2.tex, sections_v2/, tables_v2/, figures_iclr/, numbers_v2*.tex, references_v2.bib, CHANGES_v2.md and analysis/paper_assets/ (+ its tests). Three adversarial reviewers audited the manuscript (claims vs raw records; blue markup + writing + derivation correctness; build + figures). Verify each finding below against the files and the data; fix the real ones at the SOURCE (a wrong number = fix the asset code and regenerate, never hand-edit a macro), keep the blue-markup rules, recompile until clean (no errors, no undefined refs, main text <= 9 pages), update CHANGES_v2.md, and say briefly why for any finding you reject.
${JSON.stringify(serious, null, 1)}
Reviewer summaries: ${JSON.stringify(reviews.map(r => r.summary))}
${TAIL}`, { label: 'fix', phase: 'Fix', model: 'opus' })
return { plan, drafts, integrate, reviews, fix }
