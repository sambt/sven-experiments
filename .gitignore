**/__pycache__/
**/.ipynb_checkpoints/
*.egg-info/
slurm_logs/
outputs/
**/.DS_STORE

# Experiment outputs (live on labstore; experiment_results is a symlink there)
experiment_results
experiment_results/
experiment_results_jax/
profile_results/

# Reviews / submission PDF (not for the public repo)
rev*.md
sven_submission.pdf

# Vendored subrepo (its own git) and local envs / scratch
sven/
.venv
uv_cache/

# Assorted local scratch / agent working dirs
notebooks_new
notebooks_new/
agent_lab
agent_lab/
claude_experiments/
sven_experiments/

# one-off validation scripts
scratch_*.py

# local dataset caches (downloaded on demand)
torch_datasets/
profile_results_v2/
# the re-measured profile (ANALYSIS_PLAN.md 7.4); 720 JSONs, ~34 MB
profile_results_v3/

slurm_logs/
experiment_results_legacy_2026-09-18

# the ICLR manuscript is its own (Overleaf-synced) git repo
iclr_manuscript/
